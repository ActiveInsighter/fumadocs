# Fumadocs

A standalone Next.js documentation site built with Fumadocs and selectively styled with Animate UI-inspired navigation, controls, and motion.

## Workflow run IDs

GitHub Actions runs use the reusable `.github/actions/run-metadata` composite action. It records the current run ID, keeps the latest run history as an artifact, writes a job summary, and maintains the `Workflow run index` issue.
