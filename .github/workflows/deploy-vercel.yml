name: Vercel Deployment Information

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    name: Native Vercel Git Integration
    runs-on: ubuntu-24.04
    steps:
      - name: Deployment configuration
        run: |
          echo "Deployments are handled by Vercel's native Git integration."
          echo "Connect this repository in Vercel to enable preview deployments for branches and production deployments for main."
