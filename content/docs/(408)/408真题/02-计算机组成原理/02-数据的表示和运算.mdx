---
title: "第 2 章 数据的表示和运算"
---

## 2.1 数制与编码

1. 【2013】用海明码对长度为 8 位的数据进行检错和纠错时，若能纠正一位错误，则校验位数至少为（ ）。

A. 2  
B. 3  
C. 4  
D. 5  


<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 设数据位数为 $\displaystyle n=8$，校验位数为 $\displaystyle r$。若海明码能够纠正一位错误，需要满足

$$
\displaystyle 2^r\geq n+r+1.
$$

当 $\displaystyle r=3$ 时，$\displaystyle 2^3=8<8+3+1$，不满足；当 $\displaystyle r=4$ 时，$\displaystyle 2^4=16\geq 8+4+1$，满足。因此校验位至少为 4 位，选 C。




</details>

2. 【2018】冯·诺依曼结构计算机中数据采用二进制编码表示，其主要原因是（ ）。

I. 二进制的运算规则简单  
II. 制造两个稳态的物理器件较容易  
III. 便于用逻辑门电路实现算术运算

A. 仅 I、II  
B. 仅 I、III  
C. 仅 II、III  
D. I、II 和 III  


<details>
<summary>查看答案与解析</summary>

**答案：** D

**解析：** 

二进制适合计算机使用，主要有以下三点原因：

1. 二进制只有 $\displaystyle 0$ 和 $\displaystyle 1$ 两种状态，算术运算和逻辑运算规则都比较简单；
2. 现实中的电子器件较容易形成高电平、低电平或导通、截止等两个稳定状态，抗干扰能力也较强；
3. 二进制运算可直接由与门、或门、非门、异或门等逻辑门电路实现。

因此 I、II、III 均正确，选择 D。




</details>

## 2.2 整数表示和运算

1. 【2010】假定有 4 个整数用 8 位补码分别表示 $\displaystyle r_1=\text{FEH}$，$\displaystyle r_2=\text{F2H}$，$\displaystyle r_3=\text{90H}$，$\displaystyle r_4=\text{F8H}$。若将运算结果存放在一个 8 位寄存器中，则下列运算中会发生溢出的是（ ）。

A. $\displaystyle r_1\times r_2$  
B. $\displaystyle r_2\times r_3$  
C. $\displaystyle r_1\times r_4$  
D. $\displaystyle r_2\times r_4$  


<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 

8 位补码的表示范围为

$$
\displaystyle -128\sim 127.
$$

各机器数对应的真值为

$$
\displaystyle r_1=\text{FEH}=-2,
$$

$$
\displaystyle r_2=\text{F2H}=-14,
$$

$$
\displaystyle r_3=\text{90H}=-112,
$$

$$
\displaystyle r_4=\text{F8H}=-8.
$$

分别计算：

- A：$\displaystyle (-2)\times(-14)=28$，未溢出；
- B：$\displaystyle (-14)\times(-112)=1568>127$，发生溢出；
- C：$\displaystyle (-2)\times(-8)=16$，未溢出；
- D：$\displaystyle (-14)\times(-8)=112$，未溢出。

故选择 B。




</details>

2. 【2013】某字长为 8 位的计算机中，已知整型变量 $\displaystyle x$ 和 $\displaystyle y$ 的机器数分别为 $\displaystyle [x]_{\text{补}}=11110100$、$\displaystyle [y]_{\text{补}}=10110000$。若整型变量 $\displaystyle z=2x+y/2$，则 $\displaystyle z$ 的机器数为（ ）。

A. 1100 0000  
B. 0010 0100  
C. 1010 1010  
D. 溢出  


<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 

由补码求真值：

$$
\displaystyle x=11110100_2=-12,
$$

$$
\displaystyle y=10110000_2=-80.
$$

乘以 $\displaystyle 2$ 相当于左移一位，除以 $\displaystyle 2$ 对带符号补码整数通常采用算术右移一位，因此

$$
\displaystyle 2x=-24,
$$

$$
\displaystyle \frac{y}{2}=-40.
$$

于是

$$
\displaystyle z=2x+\frac{y}{2}=-24-40=-64.
$$

$\displaystyle -64$ 的 8 位补码为

$$
\displaystyle 11000000_2.
$$

因此选择 A。




</details>

3. 【2014】若 $\displaystyle x=103$，$\displaystyle y=-25$，则下列表达式采用 8 位定点补码运算实现时，会发生溢出的是（ ）。

A. $\displaystyle x+y$  
B. $\displaystyle -x+y$  
C. $\displaystyle x-y$  
D. $\displaystyle -x-y$  


<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 

8 位补码可表示的范围为

$$
\displaystyle -128\sim 127.
$$

逐项计算：

$$
\displaystyle x+y=103-25=78,
$$

$$
\displaystyle -x+y=-103-25=-128,
$$

$$
\displaystyle x-y=103-(-25)=128,
$$

$$
\displaystyle -x-y=-103-(-25)=-78.
$$

其中只有 $\displaystyle 128$ 超过最大可表示正数 $\displaystyle 127$，因此 $\displaystyle x-y$ 会发生溢出，选择 C。




</details>

4. 【2015】由 3 个“1”和 5 个“0”组成的 8 位二进制补码，能表示的最小整数是（ ）。

A. $\displaystyle -126$  
B. $\displaystyle -125$  
C. $\displaystyle -32$  
D. $\displaystyle -3$  


<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 

要表示尽可能小的负数，最高位必须为 $\displaystyle 1$。8 位补码的权值为

$$
\displaystyle -2^7,2^6,2^5,\ldots,2^1,2^0.
$$

总共只能有 3 个“1”，除符号位外还剩两个“1”。为了使真值最小，应让这两个“1”放在权值最小的位置，即最低两位：

$$
\displaystyle 10000011_2=-128+2+1=-125.
$$

故选择 B。




</details>

5. 【2018】假定带符号整数采用补码表示，若 int 型变量 $\displaystyle x$ 和 $\displaystyle y$ 的机器数分别是 FFFF FFDFH 和 0000 0041H，则 $\displaystyle x$、$\displaystyle y$ 的值及 $\displaystyle x-y$ 的机器数分别是（ ）。

A. $\displaystyle x=-65$，$\displaystyle y=41$，$\displaystyle x-y$ 的机器数溢出  
B. $\displaystyle x=-33$，$\displaystyle y=65$，$\displaystyle x-y$ 的机器数为 FFFF FF9DH  
C. $\displaystyle x=-33$，$\displaystyle y=65$，$\displaystyle x-y$ 的机器数为 FFFF FF9EH  
D. $\displaystyle x=-65$，$\displaystyle y=41$，$\displaystyle x-y$ 的机器数为 FFFF FF96H  


<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 

$\displaystyle \text{FFFF FFDFH}$ 为负数补码。其绝对值可通过按位取反再加 $\displaystyle 1$ 得到：

$$
\displaystyle \text{FFFF FFDFH}\xrightarrow{\text{取反}}\text{0000 0020H}\xrightarrow{+1}\text{0000 0021H}=33.
$$

因此

$$
\displaystyle x=-33.
$$

而

$$
\displaystyle y=\text{0000 0041H}=65.
$$

所以

$$
\displaystyle x-y=-33-65=-98.
$$

$\displaystyle 98=\text{0000 0062H}$，取补码得

$$
\displaystyle \text{FFFF FF9EH}.
$$

故选择 C。




</details>

6. 【2018】整数 $\displaystyle x$ 的机器数为 11011000，分别对 $\displaystyle x$ 进行逻辑右移 1 位和算术右移 1 位操作，得到的机器数分别是（ ）。

A. 1110 1100，1110 1100  
B. 0110 1100，1110 1100  
C. 1110 1100，0110 1100  
D. 0110 1100，0110 1100  


<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 

逻辑右移时，最高位一律补 $\displaystyle 0$：

$$
\displaystyle 11011000\xrightarrow{\text{逻辑右移 }1\text{ 位}}01101100.
$$

算术右移用于带符号数，为保持符号不变，最高位补原符号位。由于原最高位为 $\displaystyle 1$，故补 $\displaystyle 1$：

$$
\displaystyle 11011000\xrightarrow{\text{算术右移 }1\text{ 位}}11101100.
$$

因此选择 B。

> 注：原题机器数漏写了一位，结合选项应为 8 位机器数 11011000。




</details>

7. 【2021】已知带符号整数用补码表示，变量 $\displaystyle x$、$\displaystyle y$、$\displaystyle z$ 的机器数分别为 FFFDH、FFDFH、7FFCH，下列结论正确的是（ ）。

A. 若 $\displaystyle x$、$\displaystyle y$ 和 $\displaystyle z$ 为无符号整数，则 $\displaystyle z<x<y$  
B. 若 $\displaystyle x$、$\displaystyle y$ 和 $\displaystyle z$ 为无符号整数，则 $\displaystyle x<y<z$  
C. 若 $\displaystyle x$、$\displaystyle y$ 和 $\displaystyle z$ 为带符号整数，则 $\displaystyle x<y<z$  
D. 若 $\displaystyle x$、$\displaystyle y$ 和 $\displaystyle z$ 为带符号整数，则 $\displaystyle y<x<z$  


<details>
<summary>查看答案与解析</summary>

**答案：** D

**解析：** 

若按无符号整数解释：

$$
\displaystyle x=\text{FFFDH}=65533,
$$

$$
\displaystyle y=\text{FFDFH}=65503,
$$

$$
\displaystyle z=\text{7FFCH}=32764,
$$

故应有

$$
\displaystyle z<y<x,
$$

A、B 均错误。

若按 16 位补码解释：

$$
\displaystyle x=-3,\qquad y=-33,\qquad z=32764.
$$

因此

$$
\displaystyle y<x<z,
$$

选择 D。




</details>

8. 【2022】32 位补码所能表示的整数范围是（ ）。

A. $\displaystyle -2^{32}\sim 2^{31}-1$  
B. $\displaystyle -2^{31}\sim 2^{31}-1$  
C. $\displaystyle -2^{32}\sim 2^{32}-1$  
D. $\displaystyle -2^{31}\sim 2^{32}-1$  


<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 

$\displaystyle n$ 位补码的表示范围为

$$
\displaystyle -2^{n-1}\sim 2^{n-1}-1.
$$

当 $\displaystyle n=32$ 时，范围为

$$
\displaystyle -2^{31}\sim 2^{31}-1.
$$

故选择 B。




</details>

## 2.3 浮点数的表示和运算

1. 【2009】浮点数加、减运算过程一般包括对阶、尾数运算、规格化、舍入和判溢出等步骤。设浮点数的阶码和尾数均采用补码表示，且位数分别为 5 位和 7 位（均含 2 位符号位）。若有两个数 $\displaystyle X=2^7\times \dfrac{29}{32}$，$\displaystyle Y=2^5\times \dfrac{5}{8}$，则用浮点加法计算 $\displaystyle X+Y$ 的最终结果是（ ）。

A. 00111 1100010  
B. 00111 0100010  
C. 01000 0010001  
D. 发生溢出  


<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 

先将两个数写成二进制规格化形式：

$$
\displaystyle X=2^7\times \frac{29}{32}=2^7\times 0.11101_2,
$$

$$
\displaystyle Y=2^5\times \frac{5}{8}=2^5\times 0.101_2.
$$

对阶时应使阶码相同。将 $\displaystyle Y$ 的阶码由 $\displaystyle 5$ 调整为 $\displaystyle 7$，尾数右移两位：

$$
\displaystyle Y=2^7\times 0.00101_2.
$$

尾数相加：

$$
\displaystyle 0.11101_2+0.00101_2=1.00010_2.
$$

结果出现尾数溢出，需要右规一位并将阶码加 $\displaystyle 1$：

$$
\displaystyle X+Y=2^8\times 0.10001_2.
$$

阶码 $\displaystyle 8$ 的 5 位双符号位补码为 01000，尾数 $\displaystyle 0.10001_2$ 的 7 位双符号位补码为 0010001，因此最终机器数为

$$
\displaystyle 01000\ 0010001.
$$

故选择 C。




</details>

2. 【2011】float 型数据通常用 IEEE 754 单精度浮点数格式表示。若编译器将 float 型变量 $\displaystyle x$ 分配到一个 32 位浮点寄存器 FR1 中，且 $\displaystyle x=-8.25$，则 FR1 的内容是（ ）。

A. C104 0000H  
B. C242 0000H  
C. C184 0000H  
D. C1C2 0000H  


<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 

先将 $\displaystyle 8.25$ 化为二进制：

$$
\displaystyle 8.25=1000.01_2=1.00001_2\times 2^3.
$$

因此：

- 符号位 $\displaystyle S=1$；
- 实际指数为 $\displaystyle 3$，移码阶码为

$$
\displaystyle E=3+127=130=10000010_2;
$$

- 尾数字段为小数点后的部分

$$
\displaystyle 00001000000000000000000.
$$

故 IEEE 754 单精度机器数为

$$
\displaystyle 1\ 10000010\ 00001000000000000000000,
$$

写成十六进制为

$$
\displaystyle \text{C104 0000H}.
$$

故选择 A。




</details>

3. 【2012】float 类型（即 IEEE 754 单精度浮点数格式）能表示的最大正整数是（ ）。

A. $\displaystyle 2^{126}-2^{103}$  
B. $\displaystyle 2^{127}-2^{104}$  
C. $\displaystyle 2^{127}-2^{103}$  
D. $\displaystyle 2^{128}-2^{104}$  


<details>
<summary>查看答案与解析</summary>

**答案：** D

**解析：** 

IEEE 754 单精度浮点数中：

- 阶码字段为 8 位；
- 阶码全为 $\displaystyle 1$ 时用于表示无穷大和 NaN；
- 最大有限数的阶码字段为 254，对应实际指数

$$
\displaystyle 254-127=127;
$$

- 尾数字段全为 $\displaystyle 1$，有效数为

$$
\displaystyle 1.111\cdots111_2=2-2^{-23}.
$$

因此最大正有限数为

$$
\displaystyle (2-2^{-23})2^{127}=2^{128}-2^{104}.
$$

故选择 D。




</details>

4. 【2013】若某数采用 IEEE 754 单精度浮点数格式表示为 C640 0000H，则该数的值是（ ）。

A. $\displaystyle -1.5\times 2^{13}$  
B. $\displaystyle -1.5\times 2^{12}$  
C. $\displaystyle -0.5\times 2^{13}$  
D. $\displaystyle -0.5\times 2^{12}$  


<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 

将 C640 0000H 写成二进制：

$$
\displaystyle 11000110010000000000000000000000.
$$

按 IEEE 754 单精度格式划分为

$$
\displaystyle 1\ \big|\ 10001100\ \big|\ 10000000000000000000000.
$$

因此：

- 符号位为 $\displaystyle 1$，表示负数；
- 阶码字段 $\displaystyle 10001100_2=140$，实际指数为

$$
\displaystyle 140-127=13;
$$

- 有效数为

$$
\displaystyle 1.1_2=1.5.
$$

所以该数为

$$
\displaystyle -1.5\times 2^{13}.
$$

故选择 A。




</details>

5. 【2014】float 型数据常用 IEEE 754 单精度浮点格式表示。假设两个 float 型变量 $\displaystyle x$ 和 $\displaystyle y$ 分别存放在 32 位寄存器 $\displaystyle f_1$ 和 $\displaystyle f_2$ 中，若 $\displaystyle f_1=\text{CC90 0000H}$、$\displaystyle f_2=\text{B0C0 0000H}$，则 $\displaystyle x$ 和 $\displaystyle y$ 之间的关系为（ ）。

A. $\displaystyle x<y$ 且符号相同  
B. $\displaystyle x<y$ 且符号不同  
C. $\displaystyle x>y$ 且符号相同  
D. $\displaystyle x>y$ 且符号不同  


<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 

两个机器数的最高位都为 $\displaystyle 1$，因此 $\displaystyle x$ 和 $\displaystyle y$ 均为负数，符号相同。

对于同为负数的规格化浮点数，绝对值越大，真值反而越小。比较阶码即可看出：

- $\displaystyle \text{CC90 0000H}$ 的阶码明显大于 $\displaystyle \text{B0C0 0000H}$ 的阶码，因此 $\displaystyle |x|>|y|$；
- 又因为二者均为负数，所以

$$
\displaystyle x<y.
$$

故选择 A。




</details>

6. 【2015】下列有关浮点数加减运算的叙述中，正确的是（ ）。

I. 对阶操作不会引起阶码上溢或下溢  
II. 右规和尾数舍入都可能引起阶码上溢  
III. 左规时可能引起阶码下溢  
IV. 尾数溢出时，结果不一定溢出

A. 仅 II、III  
B. 仅 I、II、IV  
C. 仅 I、III、IV  
D. I、II、III、IV  


<details>
<summary>查看答案与解析</summary>

**答案：** D

**解析：** 

逐项判断：

- I 正确。对阶时通常把小阶数的尾数右移，使其阶码增大到较大阶数；最终阶码直接取原来较大的阶码，不会因为对阶本身产生新的阶码越界。
- II 正确。右规需要使阶码加 $\displaystyle 1$；舍入也可能使尾数产生进位，进而再次右规并使阶码加 $\displaystyle 1$，两者都可能造成阶码上溢。
- III 正确。左规需要使阶码减 $\displaystyle 1$，连续左规可能导致阶码下溢。
- IV 正确。尾数运算发生溢出时，通常可通过右规处理；只有右规后阶码超出可表示范围，整个浮点结果才真正溢出。

因此四项均正确，选择 D。




</details>

7. 【2018】IEEE 754 单精度浮点格式表示的数中，最小的规格化正数是（ ）。

A. $\displaystyle 1.0\times 2^{-126}$  
B. $\displaystyle 1.0\times 2^{-127}$  
C. $\displaystyle 1.0\times 2^{-128}$  
D. $\displaystyle 1.0\times 2^{-149}$  


<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 

IEEE 754 单精度规格化数的最小非零阶码字段为

$$
\displaystyle 00000001_2.
$$

其实际指数为

$$
\displaystyle 1-127=-126.
$$

取最小规格化尾数 $\displaystyle 1.0$，得到最小规格化正数

$$
\displaystyle 1.0\times 2^{-126}.
$$

$\displaystyle 2^{-149}$ 是最小非规格化正数，不是规格化数。故选择 A。




</details>

8. 【2020】已知带符号整数用补码表示，float 型数据用 IEEE 754 标准表示，假定变量 $\displaystyle x$ 的类型只能是 int 或 float，当 $\displaystyle x$ 的机器数为 C800 0000H 时，$\displaystyle x$ 的值可能是（ ）。

A. $\displaystyle -7\times 2^{27}$  
B. $\displaystyle -2^{16}$  
C. $\displaystyle 2^{17}$  
D. $\displaystyle 25\times 2^{27}$  


<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 

若把 C800 0000H 解释为 32 位 int 补码，则其真值为

$$
\displaystyle \text{C800 0000H}-2^{32}=-939524096=-7\times 2^{27}.
$$

若把它解释为 IEEE 754 单精度浮点数，则位模式为

$$
\displaystyle 1\ \big|\ 10010000\ \big|\ 000\cdots0.
$$

实际指数为

$$
\displaystyle 144-127=17,
$$

故浮点值为

$$
\displaystyle -1.0\times 2^{17}=-2^{17}.
$$

选项中只有 A 可能成立，故选择 A。

> 注：原文件中 B 项写成了 $\displaystyle -2^{17}$，这会导致 A、B 同时成立。依据原真题，B 项应为 $\displaystyle -2^{16}$。




</details>

9. 【2021】下列数值中，不能用 IEEE 754 浮点格式精确表示的是（ ）。

A. 1.2  
B. 1.25  
C. 2.0  
D. 2.5  


<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 

二进制浮点数能够精确表示的有限小数，其既约分数的分母必须是 $\displaystyle 2$ 的整数次幂。

$$
\displaystyle 1.25=\frac{5}{4},\qquad 2.0=2,\qquad 2.5=\frac{5}{2},
$$

这些数的分母均为 $\displaystyle 2$ 的幂，因此可以精确表示。

而

$$
\displaystyle 1.2=\frac{6}{5},
$$

分母含因子 $\displaystyle 5$，其二进制小数是无限循环的，不能用有限位 IEEE 754 浮点数精确表示。

故选择 A。




</details>

10. 【2022】$\displaystyle -0.4375$ 的 IEEE 754 单精度浮点数表示为（ ）。

A. BEE0 0000H  
B. BF60 0000H  
C. BF70 0000H  
D. C0E0 0000H  


<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 

先将绝对值化为二进制：

$$
\displaystyle 0.4375=0.0111_2=1.11_2\times 2^{-2}.
$$

因此：

- 符号位为 $\displaystyle 1$；
- 阶码字段为

$$
\displaystyle -2+127=125=01111101_2;
$$

- 尾数字段为

$$
\displaystyle 11000000000000000000000.
$$

于是机器数为

$$
\displaystyle 1\ 01111101\ 11000000000000000000000,
$$

写成十六进制为

$$
\displaystyle \text{BEE0 0000H}.
$$

故选择 A。




</details>

11. 【2023】已知 float 型变量用 IEEE 754 单精度浮点数格式表示。若 float 型变量 $\displaystyle x$ 的机器数为 8020 0000H，则 $\displaystyle x$ 的值是（ ）。

A. $\displaystyle -2^{-128}$  
B. $\displaystyle -1.01_2\times 2^{-127}$  
C. $\displaystyle -1.01_2\times 2^{-126}$  
D. 非数（NaN）  


<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 

将 8020 0000H 按 IEEE 754 单精度格式划分：

$$
\displaystyle 1\ \big|\ 00000000\ \big|\ 01000000000000000000000.
$$

符号位为 $\displaystyle 1$，阶码字段全为 $\displaystyle 0$，尾数字段不全为 $\displaystyle 0$，因此这是一个负的非规格化数，而不是 NaN。

非规格化数的数值为

$$
\displaystyle (-1)^S\times (0.f)_2\times 2^{-126}.
$$

本题尾数字段为 $\displaystyle 0.01_2$，故

$$
\displaystyle x=-0.01_2\times 2^{-126}
=-2^{-2}\times 2^{-126}
=-2^{-128}.
$$

故选择 A。




</details>

12. 【2024】某科学实验中，需要使用大量的整型参数。为了在保证表数精度的基础上提高运算速度，需要选择合理的数据表示方法。若整型参数 $\displaystyle \alpha$、$\displaystyle \beta$ 的取值范围分别为 $\displaystyle -2^{20}\sim 2^{20}$、$\displaystyle -2^{40}\sim 2^{40}$，则下列选项中，$\displaystyle \alpha$、$\displaystyle \beta$ 最适宜采用的数据表示方法分别是（ ）。

A. 32 位整数、32 位整数  
B. 单精度浮点数、单精度浮点数  
C. 32 位整数、双精度浮点数  
D. 单精度浮点数、双精度浮点数  


<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 

对于 $\displaystyle \alpha$：

$$
\displaystyle -2^{20}\le \alpha\le 2^{20},
$$

显然落在 32 位有符号整数范围

$$
\displaystyle -2^{31}\sim 2^{31}-1
$$

之内，而且使用整数表示可以保证所有整数均精确，因此 $\displaystyle \alpha$ 适合采用 32 位整数。

对于 $\displaystyle \beta$，其范围达到 $\displaystyle \pm 2^{40}$，超出 32 位整数范围。IEEE 754 单精度浮点数只有 24 位有效二进制位，不能保证该范围内所有整数都精确表示；双精度浮点数具有 53 位有效二进制位，可以精确表示绝对值不超过 $\displaystyle 2^{53}$ 的整数，因此能够保证 $\displaystyle \beta$ 的精度。

故选择 C。




</details>

## 2.4 C 语言中的类型转换

1. 【2009】一个 C 语言程序在一台 32 位机器上运行。程序中定义了三个变量 $\displaystyle x$、$\displaystyle y$、$\displaystyle z$，其中 $\displaystyle x$ 和 $\displaystyle z$ 为 int 型，$\displaystyle y$ 为 short 型。当 $\displaystyle x=127$、$\displaystyle y=-9$ 时，执行赋值语句 $\displaystyle z=x+y$ 后，$\displaystyle x$、$\displaystyle y$、$\displaystyle z$ 的机器数分别是（ ）。

A. $\displaystyle x=\text{0000007FH}$，$\displaystyle y=\text{FFF9H}$，$\displaystyle z=\text{00000076H}$  
B. $\displaystyle x=\text{0000007FH}$，$\displaystyle y=\text{FFF9H}$，$\displaystyle z=\text{FFFF0076H}$  
C. $\displaystyle x=\text{0000007FH}$，$\displaystyle y=\text{FFF7H}$，$\displaystyle z=\text{FFFF0076H}$  
D. $\displaystyle x=\text{0000007FH}$，$\displaystyle y=\text{FFF7H}$，$\displaystyle z=\text{00000076H}$  


<details>
<summary>查看答案与解析</summary>

**答案：** D

**解析：** 

$\displaystyle x=127$，其 32 位补码为

$$
\displaystyle \text{0000007FH}.
$$

$\displaystyle y=-9$，先写出 $\displaystyle 9$ 的 16 位二进制形式并取补码：

$$
\displaystyle 9=\text{0009H},
$$

$$
\displaystyle -9=\text{FFF7H}.
$$

计算 $\displaystyle x+y$ 时，short 型的 $\displaystyle y$ 先进行整型提升，符号扩展为 32 位：

$$
\displaystyle \text{FFF7H}\longrightarrow \text{FFFFFFF7H}=-9.
$$

于是

$$
\displaystyle z=127+(-9)=118=\text{00000076H}.
$$

故选择 D。




</details>

2. 【2010】假定变量 $\displaystyle i$、$\displaystyle f$ 和 $\displaystyle d$ 的数据类型分别为 int、float 和 double（int 用补码表示，float 和 double 分别用 IEEE 754 单精度和双精度浮点数格式表示），已知 $\displaystyle i=785$、$\displaystyle f=1.5678\mathrm{e}3$、$\displaystyle d=1.5\mathrm{e}100$。若在 32 位机器中执行下列关系表达式，则结果为“真”的是（ ）。

I. `i == (int)(float)i`  
II. `f == (float)(int)f`  
III. `f == (float)(double)f`  
IV. `d + f - d == f`

A. 仅 I、II  
B. 仅 I、III  
C. 仅 II、III  
D. 仅 III、IV  


<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 

逐项判断：

- I：$\displaystyle 785$ 的二进制有效位数远小于 24 位，能够被 float 精确表示，转成 float 后再转回 int 仍为 $\displaystyle 785$，故为真。
- II：$\displaystyle f$ 约为 $\displaystyle 1567.8$，强制转换为 int 时小数部分被截去，得到 $\displaystyle 1567$；再转回 float 后为 $\displaystyle 1567.0$，与原来的 $\displaystyle f$ 不相等，故为假。
- III：float 转换为 double 时数值可被精确扩展，再转换回 float 能恢复原来的 float 位模式，故为真。
- IV：$\displaystyle d$ 的数量级约为 $\displaystyle 10^{100}$，远大于 $\displaystyle f$。计算 $\displaystyle d+f$ 时，$\displaystyle f$ 因有效位不足被舍去，故 $\displaystyle d+f=d$，随后

$$
\displaystyle d+f-d=0\ne f,
$$

故为假。

因此只有 I、III 为真，选择 B。




</details>

3. 【2012】假定编译器规定 int 和 short 类型长度分别为 32 位和 16 位，执行下列 C 语言语句：

```c
unsigned short x = 65530;
unsigned int y = x;
```

得到 $\displaystyle y$ 的机器数为（ ）。

A. 0000 7FFAH  
B. 0000 FFFAH  
C. FFFF 7FFAH  
D. FFFF FFFAH  


<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 

$$
\displaystyle 65530=2^{16}-6=\text{FFFAH}.
$$

$\displaystyle x$ 是 unsigned short，转换为 32 位 unsigned int 时进行零扩展，而不是符号扩展：

$$
\displaystyle \text{FFFAH}\longrightarrow \text{0000 FFFAH}.
$$

故选择 B。




</details>

4. 【2016】有如下 C 语言程序段：

```c
short si = -32767;
unsigned short usi = si;
```

执行上述两条语句后，`usi` 的值为（ ）。

A. $\displaystyle -32767$  
B. $\displaystyle 32767$  
C. $\displaystyle 32768$  
D. $\displaystyle 32769$  


<details>
<summary>查看答案与解析</summary>

**答案：** D

**解析：** 

$\displaystyle -32767$ 的 16 位补码为

$$
\displaystyle 1000000000000001_2=\text{8001H}.
$$

赋值给 unsigned short 后，位模式保持为 8001H，只是改用无符号方式解释：

$$
\displaystyle \text{8001H}=2^{15}+1=32769.
$$

也可利用有符号数转换为同宽无符号数的模运算规则：

$$
\displaystyle -32767\bmod 2^{16}=65536-32767=32769.
$$

故选择 D。




</details>

5. 【2019】考虑以下 C 语言代码，执行程序段后，`si` 的值是（ ）。

```c
unsigned short usi = 65535;
short si = usi;
```

A. $\displaystyle -1$  
B. $\displaystyle -32767$  
C. $\displaystyle -32768$  
D. $\displaystyle -65535$  


<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 

unsigned short 的 $\displaystyle 65535$ 对应 16 位机器数

$$
\displaystyle \text{FFFFH}.
$$

将其赋给同为 16 位的 short 后，通常保持低 16 位不变，再按补码解释。FFFFH 作为 16 位补码表示

$$
\displaystyle -1.
$$

故选择 A。




</details>

6. 【2023】若 short 型变量 $\displaystyle x=-8190$，则 $\displaystyle x$ 的机器数是（ ）。

A. E002H  
B. E001H  
C. 9FFFH  
D. 9FFEH  


<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 

先将 $\displaystyle 8190$ 写成十六进制：

$$
\displaystyle 8190=\text{1FFEH}.
$$

求 16 位补码：

$$
\displaystyle \text{1FFEH}\xrightarrow{\text{按位取反}}\text{E001H}\xrightarrow{+1}\text{E002H}.
$$

因此 $\displaystyle -8190$ 的机器数为 E002H，选择 A。




</details>

7. 【2024】C 语言代码如下。执行下列代码段后，$\displaystyle j$ 的值是（ ）。

```c
int i = 32777;
short si = i;
int j = si;
```

A. $\displaystyle -32777$  
B. $\displaystyle -32759$  
C. $\displaystyle 32759$  
D. $\displaystyle 32777$  


<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 

$$
\displaystyle 32777=\text{00008009H}.
$$

将 32 位 int 转换为 16 位 short 时保留低 16 位，得到

$$
\displaystyle \text{8009H}.
$$

8009H 的最高位为 $\displaystyle 1$，按 16 位补码解释：

$$
\displaystyle \text{8009H}-2^{16}=32777-65536=-32759.
$$

再将 short 型的 `si` 转换为 int 时进行符号扩展，数值仍为 $\displaystyle -32759$，故选择 B。




</details>

8. 【2011】假定在一个 8 位字长的计算机中运行如下类 C 程序段：

```c
unsigned int x = 134;
unsigned int y = 246;
int m = x;
int n = y;
unsigned int z1 = x - y;
unsigned int z2 = x + y;
int k1 = m - n;
int k2 = m + n;
```

若编译器将 8 个 8 位寄存器 R1～R8 分别分配给变量 $\displaystyle x$、$\displaystyle y$、$\displaystyle m$、$\displaystyle n$、$\displaystyle z_1$、$\displaystyle z_2$、$\displaystyle k_1$ 和 $\displaystyle k_2$。请回答下列问题（带符号整数用补码表示）：

（1）执行上述程序段后，寄存器 R1、R5 和 R6 的内容分别是什么？用十六进制表示。  
（2）执行上述程序段后，变量 $\displaystyle m$ 和 $\displaystyle k_1$ 的值分别是多少？用十进制表示。  
（3）上述程序段涉及带符号整数加/减、无符号整数加/减运算，这四种运算能否利用同一个加法器及辅助电路实现？简述理由。  
（4）计算机内部如何判断带符号整数加/减运算的结果是否发生溢出？上述程序段中，哪些带符号整数运算语句的执行结果会发生溢出？


<details>
<summary>查看答案与解析</summary>

**答案：** 

（1）R1、R5、R6 的内容依次为 86H、90H、7CH。  
（2）$\displaystyle m=-122$，$\displaystyle k_1=-112$。  
（3）可以使用同一个加法器及辅助电路实现。  
（4）`k2 = m + n` 发生溢出，`k1 = m - n` 不发生溢出。

**解析：** 


</details>

### （1）求 R1、R5 和 R6 的内容

8 位无符号整数按模 $\displaystyle 2^8=256$ 运算。

变量 $\displaystyle x=134$，故

$$
\displaystyle 134=10000110_2=\text{86H},
$$

因此

$$
\displaystyle R1=\text{86H}.
$$

对于 $\displaystyle z_1=x-y$：

$$
\displaystyle z_1=134-246=-112.
$$

无符号运算结果按模 $\displaystyle 256$ 截断：

$$
\displaystyle -112\bmod 256=144=10010000_2=\text{90H}.
$$

因此

$$
\displaystyle R5=\text{90H}.
$$

对于 $\displaystyle z_2=x+y$：

$$
\displaystyle z_2=134+246=380.
$$

按模 $\displaystyle 256$ 截断：

$$
\displaystyle 380\bmod 256=124=01111100_2=\text{7CH}.
$$

因此

$$
\displaystyle R6=\text{7CH}.
$$

### （2）求 $\displaystyle m$ 和 $\displaystyle k_1$

在本题所采用的机器级解释中，将 8 位无符号数赋给 8 位带符号数时，寄存器中的位模式保持不变。

$\displaystyle x$ 的位模式为

$$
\displaystyle 10000110_2.
$$

将其按 8 位补码解释：

$$
\displaystyle m=134-256=-122.
$$

同理，$\displaystyle y=246$ 的位模式为

$$
\displaystyle 11110110_2=\text{F6H},
$$

按补码解释为

$$
\displaystyle n=246-256=-10.
$$

所以

$$
\displaystyle k_1=m-n=-122-(-10)=-112.
$$

$\displaystyle -112$ 在 8 位补码范围 $\displaystyle -128\sim127$ 内，机器数为 90H。

### （3）能否共用一个加法器

可以。

无论操作数按有符号数还是无符号数解释，加法器对两个 $\displaystyle n$ 位二进制位串执行的低 $\displaystyle n$ 位加法完全相同。减法也可以化为补码加法：

$$
\displaystyle A-B=A+\overline{B}+1.
$$

因此带符号加法、带符号减法、无符号加法和无符号减法均可复用同一个 $\displaystyle n$ 位加法器。它们的差别主要在于：

- 无符号运算根据进位或借位标志 CF 判断越界；
- 带符号运算根据溢出标志 OF 判断结果是否超出补码范围；
- 减法时还需要对第二个操作数取反并设置最低位进位输入为 $\displaystyle 1$。

### （4）溢出判断及本题结果

带符号加法 $\displaystyle A+B$ 的溢出条件是：两个操作数符号相同，而结果符号与操作数符号不同。设符号位分别为 $\displaystyle A_s$、$\displaystyle B_s$、$\displaystyle S_s$，则

$$
\displaystyle OF=\overline{A_s\oplus B_s}\,(A_s\oplus S_s).
$$

带符号减法 $\displaystyle A-B$ 的溢出条件是：$\displaystyle A$、$\displaystyle B$ 符号不同，且结果符号与 $\displaystyle A$ 不同，即

$$
\displaystyle OF=(A_s\oplus B_s)(A_s\oplus S_s).
$$

也可以利用符号位的进位判断：符号位的进位输入与进位输出不同时，$\displaystyle OF=1$。

本题中：

$$
\displaystyle k_1=m-n=-122-(-10)=-112,
$$

结果可表示，不溢出。

而

$$
\displaystyle k_2=m+n=-122+(-10)=-132<-128,
$$

超出 8 位补码的最小值。机器只保留低 8 位，得到 7CH，即按补码解释为 $\displaystyle 124$，两个负数相加却得到正数，因此发生溢出。

故只有语句

```c
int k2 = m + n;
```

发生带符号溢出。



9. 【2017】已知

$$
\displaystyle f(n)=\sum_{i=0}^{n}2^i=2^{n+1}-1=(11\cdots1)_2,
$$

计算 $\displaystyle f(n)$ 的 C 语言函数 $\displaystyle f_1$ 如下：

```c
int f1(unsigned n) {
    int sum = 1, power = 1;
    for (unsigned i = 0; i <= n - 1; i++) {
        power *= 2;
        sum += power;
    }
    return sum;
}
```

将 $\displaystyle f_1$ 中的 int 都改为 float，可得到计算 $\displaystyle f(n)$ 的另一个函数 $\displaystyle f_2$。假设 unsigned 和 int 型数据都占 32 位，float 采用 IEEE 754 单精度标准。请回答下列问题：

（1）当 $\displaystyle n=0$ 时，$\displaystyle f_1$ 会出现死循环，为什么？若将 $\displaystyle f_1$ 中的变量 $\displaystyle i$ 和 $\displaystyle n$ 都定义为 int 型，则 $\displaystyle f_1$ 是否还会出现死循环？为什么？  
（2）$\displaystyle f_1(23)$ 和 $\displaystyle f_2(23)$ 的返回值是否相等？机器数各是什么？用十六进制表示。  
（3）$\displaystyle f_1(24)$ 和 $\displaystyle f_2(24)$ 的返回值分别为 33554431 和 33554432.0，为什么不相等？  
（4）$\displaystyle f(31)=2^{32}-1$，而 $\displaystyle f_1(31)$ 的返回值却为 $\displaystyle -1$，为什么？若使 $\displaystyle f_1(n)$ 的返回值与 $\displaystyle f(n)$ 相等，则最大的 $\displaystyle n$ 是多少？  
（5）$\displaystyle f_2(127)$ 的机器数为 7F80 0000H，对应的值是什么？若使 $\displaystyle f_2(n)$ 的结果不溢出，则最大的 $\displaystyle n$ 是多少？若使 $\displaystyle f_2(n)$ 的结果精确（无舍入），则最大的 $\displaystyle n$ 是多少？


<details>
<summary>查看答案与解析</summary>

**答案：** 

（1）`unsigned` 的 $\displaystyle n-1$ 在 $\displaystyle n=0$ 时回绕为 $\displaystyle 2^{32}-1$，循环变量 $\displaystyle i$ 也会回绕，因此循环条件始终成立；若 $\displaystyle i$、$\displaystyle n$ 均改为 int，则不会死循环。  
（2）数值相等，均为 16777215；$\displaystyle f_1(23)$ 的机器数为 00FF FFFFH，$\displaystyle f_2(23)$ 的机器数为 4B7F FFFFH。  
（3）单精度浮点数有效位有限，33554431 需要 25 位有效二进制位，按“舍入到最近、偶数优先”得到 33554432.0。  
（4）$\displaystyle f_1(31)$ 的低 32 位全为 $\displaystyle 1$，按 int 补码解释为 $\displaystyle -1$；最大 $\displaystyle n=30$。  
（5）7F80 0000H 表示正无穷大；不溢出的最大 $\displaystyle n=126$；结果精确的最大 $\displaystyle n=23$。

**解析：** 


</details>

### （1）$\displaystyle n=0$ 时的死循环

$\displaystyle n$ 是 unsigned 类型。当 $\displaystyle n=0$ 时，表达式 $\displaystyle n-1$ 不是 $\displaystyle -1$，而是发生模 $\displaystyle 2^{32}$ 回绕：

$$
\displaystyle n-1=2^{32}-1=\text{FFFF FFFFH}.
$$

循环条件变成

```c
i <= 0xFFFFFFFFU
```

$\displaystyle i$ 也是 unsigned，取值范围为 $\displaystyle 0\sim2^{32}-1$。当 $\displaystyle i$ 增加到最大值后再加 $\displaystyle 1$，又回绕为 $\displaystyle 0$，所以条件始终为真，程序出现死循环。

若将 $\displaystyle i$、$\displaystyle n$ 都改为 int，则 $\displaystyle n=0$ 时

$$
\displaystyle n-1=-1.
$$

初始 $\displaystyle i=0$，判断 $\displaystyle 0\le -1$ 为假，循环一次也不执行，不会出现死循环。

### （2）$\displaystyle f_1(23)$ 与 $\displaystyle f_2(23)$

$$
\displaystyle f(23)=2^{24}-1=16777215.
$$

该数的二进制表示恰好是 24 个连续的 $\displaystyle 1$。IEEE 754 单精度浮点数具有 24 位有效二进制精度，包括隐藏的最高位，因此可以精确表示该数。

所以两函数返回值在数值上相等。

$\displaystyle f_1(23)$ 是 int，机器数为

$$
\displaystyle 2^{24}-1=\text{00FF FFFFH}.
$$

$\displaystyle f_2(23)$ 的规格化形式为

$$
\displaystyle 1.111\cdots111_2\times2^{23},
$$

其中尾数字段为 23 个 $\displaystyle 1$，阶码为

$$
\displaystyle 23+127=150=10010110_2.
$$

因此机器数为

$$
\displaystyle \text{4B7F FFFFH}.
$$

### （3）为什么 $\displaystyle f_2(24)$ 被舍入

$$
\displaystyle f(24)=2^{25}-1=33554431.
$$

其二进制表示包含 25 个有效位，而单精度 float 只能保留 24 个有效位。在 $\displaystyle 2^{24}$ 附近，相邻可表示浮点数的间隔为

$$
\displaystyle 2^{24-23}=2.
$$

所以相邻的可表示数是

$$
\displaystyle 33554430\quad\text{和}\quad33554432.
$$

$\displaystyle 33554431$ 恰好位于二者中间。IEEE 754 默认采用“舍入到最近，距离相同时取尾数最低位为偶数者”的规则，因此舍入为

$$
\displaystyle 33554432.0.
$$

### （4）$\displaystyle f_1(31)$ 及其精确范围

$$
\displaystyle f(31)=2^{32}-1=\text{FFFF FFFFH}.
$$

该结果超出 32 位 int 的最大值 $\displaystyle 2^{31}-1$。机器保留低 32 位后位模式为 FFFF FFFFH，按 32 位补码解释即为

$$
\displaystyle -1.
$$

若要求 $\displaystyle f_1(n)$ 的返回值与数学上的 $\displaystyle f(n)$ 相等，需要

$$
\displaystyle 2^{n+1}-1\le2^{31}-1.
$$

因此

$$
\displaystyle n+1\le31,
$$

即

$$
\displaystyle n\le30.
$$

最大 $\displaystyle n$ 为 $\displaystyle 30$。

### （5）$\displaystyle f_2$ 的溢出范围和精确范围

IEEE 754 单精度机器数 7F80 0000H 的阶码全为 $\displaystyle 1$、尾数全为 $\displaystyle 0$、符号位为 $\displaystyle 0$，表示

$$
\displaystyle +\infty.
$$

从 $\displaystyle n=24$ 开始，舍入使得求和结果逐步成为 $\displaystyle 2^{n+1}$。当 $\displaystyle n=126$ 时，结果为

$$
\displaystyle 2^{127},
$$

仍是有限单精度数；当 $\displaystyle n=127$ 时，需要得到约 $\displaystyle 2^{128}$ 的结果，超过最大有限单精度数，舍入为正无穷大。因此不发生溢出的最大值为

$$
\displaystyle n=126.
$$

要保证结果无舍入，需要 $\displaystyle f(n)=2^{n+1}-1$ 的有效二进制位数不超过 24，即

$$
\displaystyle n+1\le24.
$$

所以结果精确的最大值为

$$
\displaystyle n=23.
$$



## 2.5 运算方法和运算电路

1. 【2011】某机器有一个标志寄存器，其中有进位/借位标志 CF、零标志 ZF、符号标志 SF 和溢出标志 OF。条件转移指令 `bgt`（无符号整数比较大于时转移）的转移条件是（ ）。

A. $\displaystyle CF+OF=1$  
B. $\displaystyle \overline{SF+ZF}=1$  
C. $\displaystyle \overline{CF+ZF}=1$  
D. $\displaystyle \overline{CF+SF}=1$  


<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 

比较两个无符号整数 $\displaystyle A$、$\displaystyle B$ 时，处理器通常执行

$$
\displaystyle A-B.
$$

若 $\displaystyle A>B$，应同时满足：

1. 结果不为 $\displaystyle 0$，即 $\displaystyle ZF=0$；
2. 减法没有产生借位，即 $\displaystyle CF=0$。

因此转移条件为

$$
\displaystyle CF=0\quad\text{且}\quad ZF=0,
$$

等价于

$$
\displaystyle \overline{CF+ZF}=1.
$$

故选择 C。

> 注：原文件中的选项上划线缺失。若没有上划线，四个选项均不能正确表达“CF 与 ZF 均为 0”。




</details>

2. 【2018】减法指令 `sub R1, R2, R3` 的功能为“$\displaystyle (R1)-(R2)\to R3$”，该指令执行后将生成进位/借位标志 CF 和溢出标志 OF。若 $\displaystyle (R1)=\text{FFFF FFFFH}$，$\displaystyle (R2)=\text{FFFF FFF0H}$，则该减法指令执行后，CF 与 OF 分别为（ ）。

A. $\displaystyle CF=0,OF=0$  
B. $\displaystyle CF=1,OF=0$  
C. $\displaystyle CF=0,OF=1$  
D. $\displaystyle CF=1,OF=1$  


<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 

按无符号数解释：

$$
\displaystyle \text{FFFF FFFFH}>\text{FFFF FFF0H},
$$

因此执行减法时不需要借位，故

$$
\displaystyle CF=0.
$$

按 32 位补码解释：

$$
\displaystyle \text{FFFF FFFFH}=-1,
$$

$$
\displaystyle \text{FFFF FFF0H}=-16.
$$

所以

$$
\displaystyle -1-(-16)=15,
$$

结果在 32 位有符号整数范围内，没有溢出，故

$$
\displaystyle OF=0.
$$

因此选择 A。




</details>

3. 【2023】已知 $\displaystyle x$、$\displaystyle y$ 为 int 类型，当 $\displaystyle x=100$、$\displaystyle y=200$ 时，执行 $\displaystyle x-y$ 指令得到的溢出标志 OF 和借位标志 CF 分别为 $\displaystyle 0$、$\displaystyle 1$。那么当 $\displaystyle x=10$、$\displaystyle y=-20$ 时，执行该指令得到的 OF 和 CF 分别是（ ）。

A. $\displaystyle OF=0,CF=0$  
B. $\displaystyle OF=0,CF=1$  
C. $\displaystyle OF=1,CF=0$  
D. $\displaystyle OF=1,CF=1$  


<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 

按带符号整数计算：

$$
\displaystyle x-y=10-(-20)=30.
$$

$\displaystyle 30$ 在 int 的表示范围内，因此不发生有符号溢出：

$$
\displaystyle OF=0.
$$

判断 CF 时按无符号数解释操作数。$\displaystyle -20$ 的 32 位补码对应一个很大的无符号数：

$$
\displaystyle 2^{32}-20.
$$

由于

$$
\displaystyle 10<2^{32}-20,
$$

执行无符号减法时需要借位，因此

$$
\displaystyle CF=1.
$$

故选择 B。




</details>

4. 【2024】下列关于整数乘法运算的叙述中，错误的是（ ）。

A. 用阵列乘法器实现的乘运算可以在一个时钟周期内完成  
B. 用 ALU 和移位器实现的乘运算无法在一个时钟周期内完成  
C. 变量与常数的乘运算可编译优化为若干条移位及加/减运算指令  
D. 两个变量的乘运算无法编译转换为移位及加法等指令的循环实现  


<details>
<summary>查看答案与解析</summary>

**答案：** D

**解析：** 

- A 正确。阵列乘法器通过组合逻辑并行产生和累加部分积，在电路延迟允许的情况下可在一个时钟周期内完成乘法。
- B 正确。若复用 ALU 和移位器，通常需要逐位判断乘数并反复执行“加法—移位”，需要多个时钟周期。
- C 正确。例如

$$
\displaystyle 10x=(8+2)x=(x\ll 3)+(x\ll 1),
$$

编译器可将常数乘法优化为移位和加减指令。
- D 错误。两个变量的乘法同样可以通过循环检查乘数各位，并用移位与加法累加部分积来实现，只是执行速度通常较慢。

故选择 D。




</details>

5. 【2020】有实现 $\displaystyle x\times y$ 的两个 C 语言函数如下：

```c
unsigned umul(unsigned x, unsigned y) { return x * y; }
int imul(int x, int y) { return x * y; }
```

假定某计算机 M 中 ALU 只能进行加减运算和逻辑运算。请回答下列问题：

（1）若 M 的指令系统中没有乘法指令，但有加法、减法和位移等指令，则在 M 上也能实现上述两个函数中的乘法运算，为什么？  
（2）若 M 的指令系统中有乘法指令，则基于 ALU、位移器、寄存器以及相应控制逻辑实现乘法指令时，控制逻辑的作用是什么？  
（3）针对以下三种情况：①没有乘法指令；②有使用 ALU 和位移器实现的乘法指令；③有使用阵列乘法器实现的乘法指令。函数 `umul（ ）` 在哪种情况下执行时间最长？哪种情况下执行时间最短？说明理由。  
（4）$\displaystyle n$ 位整数乘法指令可保存 $\displaystyle 2n$ 位乘积，当仅取低 $\displaystyle n$ 位作为乘积时，其结果可能会发生溢出。当 $\displaystyle n=32$，$\displaystyle x=2^{31}-1$，$\displaystyle y=2$ 时，带符号整数乘法指令和无符号整数乘法指令得到的 $\displaystyle x\times y$ 的 $\displaystyle 2n$ 位乘积分别是什么？用十六进制表示。此时函数 `umul（ ）` 和 `imul（ ）` 的返回结果是否溢出？对于无符号整数乘法运算，当仅取乘积的低 $\displaystyle n$ 位作为乘法结果时，如何用 $\displaystyle 2n$ 位乘积进行溢出判断？


<details>
<summary>查看答案与解析</summary>

**答案：** 

（1）整数乘法可以分解为对部分积的移位和加法，带符号乘法还可通过符号处理或补码乘法算法实现。  
（2）控制逻辑负责发出时序控制信号，判断乘数位，控制寄存器装入、移位、ALU 加减以及循环计数，并在运算完成后结束指令。  
（3）没有乘法指令时执行时间最长；采用阵列乘法器时执行时间最短。  
（4）带符号和无符号乘法得到的 64 位乘积均为 0000 0000 FFFF FFFEH。`umul()` 不溢出，`imul()` 溢出。无符号乘法仅取低 32 位时，若高 32 位不全为 $\displaystyle 0$，则发生溢出。

**解析：** 


</details>

### （1）没有乘法指令仍能实现乘法的原因

二进制乘法可以写成若干个移位后的被乘数之和。若

$$
\displaystyle y=\sum_{i=0}^{n-1}y_i2^i,
$$

其中 $\displaystyle y_i\in\{0,1\}$，则

$$
\displaystyle x\times y=\sum_{i=0}^{n-1}y_i(x\ll i).
$$

程序可以逐位检查乘数 $\displaystyle y$：当最低位为 $\displaystyle 1$ 时，将当前被乘数加入部分积；随后被乘数左移、乘数右移，循环直到所有乘数位处理完毕。因此即使没有硬件乘法指令，也可以用加法、位移和逻辑指令通过软件实现乘法。

对于带符号整数，可先记录结果符号，对绝对值做无符号乘法后再求补；也可直接采用补码乘法算法。

### （2）控制逻辑的作用

使用 ALU、移位器和寄存器实现乘法时，数据通路本身只负责执行具体的加减、移位和保存操作。控制逻辑负责协调这些部件，主要包括：

1. 将被乘数、乘数和初始部分积装入相应寄存器；
2. 检查乘数当前位，并决定本轮是否执行加法或减法；
3. 控制 ALU 的运算类型；
4. 控制被乘数、乘数或部分积寄存器的移位；
5. 维护循环计数器，判断是否已经处理完全部 $\displaystyle n$ 位；
6. 在运算完成后把结果写回目标寄存器并结束乘法指令。

因此控制逻辑本质上负责产生乘法算法各步骤所需的时序控制信号。

### （3）三种实现方式的速度比较

① 没有乘法指令时，编译器或运行库需要生成多条移位、条件判断、加法和循环控制指令。除了算术操作外，还要反复进行取指、译码和分支，因此执行时间通常最长。

② 使用 ALU 和移位器实现乘法指令时，乘法仍需经过多轮“判断—加法—移位”，但这些步骤在处理器内部由控制逻辑完成，减少了软件指令开销，速度通常快于情况①。

③ 阵列乘法器利用大量组合逻辑并行产生和压缩部分积，可在一个时钟周期或很少的流水级中完成乘法，执行时间通常最短。

因此：

- 最长：没有乘法指令；
- 最短：使用阵列乘法器实现乘法指令。

### （4）乘积、返回结果与溢出判断

给定

$$
\displaystyle x=2^{31}-1=\text{7FFF FFFFH},
$$

$$
\displaystyle y=2=\text{0000 0002H}.
$$

数学乘积为

$$
\displaystyle x\times y=(2^{31}-1)\times2=2^{32}-2=4294967294.
$$

其 64 位二进制乘积为

$$
\displaystyle \text{0000 0000 FFFF FFFEH}.
$$

因为两个操作数均为正数，无论使用带符号乘法指令还是无符号乘法指令，得到的完整 64 位位串都相同：

$$
\displaystyle \boxed{\text{0000 0000 FFFF FFFEH}}.
$$

函数 `umul()` 将低 32 位 FFFF FFFEH 按 unsigned 解释：

$$
\displaystyle \text{FFFF FFFEH}=2^{32}-2=4294967294,
$$

该值在 32 位无符号整数范围 $\displaystyle 0\sim2^{32}-1$ 内，因此 `umul()` 的返回结果不溢出。

函数 `imul()` 也只返回低 32 位 FFFF FFFEH，但按 32 位补码解释为

$$
\displaystyle -2.
$$

而真实的带符号数学乘积为 $\displaystyle 4294967294$，超出 int 的范围 $\displaystyle -2^{31}\sim2^{31}-1$，因此 `imul()` 发生溢出。

对于无符号 $\displaystyle n$ 位乘法，若保留了完整的 $\displaystyle 2n$ 位乘积，则只取低 $\displaystyle n$ 位作为结果时：

- 高 $\displaystyle n$ 位全为 $\displaystyle 0$，说明完整乘积小于 $\displaystyle 2^n$，不溢出；
- 高 $\displaystyle n$ 位不全为 $\displaystyle 0$，说明完整乘积不小于 $\displaystyle 2^n$，舍弃高位会改变数值，发生溢出。

本题完整乘积的高 32 位为 0000 0000H，所以无符号乘法不溢出。
