---
title: "第 1 章 计算机网络概述"
---

## 1.1 计算机网络体系结构

1. 【2009】在 OSI 参考模型中, 自下而上第一个提供端到端服务的层次是（ ）。

A. 数据链路层  
B. 传输层  
C. 会话层  
D. 应用层  


<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** OSI 参考模型自下而上依次为物理层、数据链路层、网络层、传输层、会话层、表示层和应用层。

物理层、数据链路层和网络层主要面向网络中的通信过程：数据链路层提供相邻结点之间的数据传输，网络层负责分组转发与路由选择。传输层位于通信子网与资源子网的接口处，首次从源主机到目的主机的角度提供端到端的进程通信服务，因此选 B。

A 项数据链路层提供的是相邻结点之间的点到点服务；C、D 两项虽然也面向端系统，但它们位于传输层之上，不是自下而上第一个提供端到端服务的层次。




</details>

2. 【2010】下列选项中, 不属于网络体系结构所描述的内容是（ ）。

A. 网络的层次  
B. 每层使用的协议  
C. 协议的内部实现细节  
D. 每层必须完成的功能  


<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 计算机网络体系结构是计算机网络各层及其协议的集合，主要描述网络如何分层、各层应完成什么功能，以及同层实体之间采用什么协议。

体系结构规定“做什么”，但不规定“具体怎样实现”。协议的内部实现细节属于实现技术，同一种网络体系结构可以采用不同的软硬件实现，因此 C 项不属于网络体系结构描述的内容。

A、B、D 三项分别对应网络的层次结构、协议以及各层功能，均属于网络体系结构的基本内容。




</details>

3. 【2011】TCP/IP 参考模型的网络层提供的是（ ）。

A. 无连接不可靠的数据报服务  
B. 无连接可靠的数据报服务  
C. 有连接不可靠的虚电路服务  
D. 有连接可靠的虚电路服务  


<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** TCP/IP 参考模型的网络层通常称为网际层，其核心协议是 IP。IP 采用数据报方式，每个 IP 分组独立选择传输路径，发送前不需要建立连接，因此属于无连接服务。

IP 只提供尽最大努力交付，不保证分组一定到达，也不保证按序到达，不负责端到端的差错恢复和重传，所以属于不可靠服务。可靠性通常由上层的 TCP 等协议实现。因此应选 A。

B 项错误在于 IP 不保证可靠传输；C、D 两项所述虚电路服务需要预先建立逻辑连接，不符合 IP 数据报服务的特点。




</details>

4. 【2013】在 OSI 参考模型中, 下列功能需由应用层的相邻层实现的是（ ）。

A. 对话管理  
B. 数据格式转换  
C. 路由选择  
D. 可靠数据传输  


<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** OSI 参考模型中，与应用层直接相邻的下一层是表示层。表示层负责处理通信双方交换信息的表示方式，其典型功能包括数据格式转换、数据压缩和加密解密等，因此选 B。

A 项对话管理属于会话层；C 项路由选择属于网络层；D 项可靠的端到端数据传输主要由传输层负责。




</details>

5. 【2014】在 OSI 参考模型中, 直接为会话层提供服务的是（ ）。

A. 应用层  
B. 表示层  
C. 传输层  
D. 网络层  


<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** OSI 参考模型中，各层通常使用下一层提供的服务，并向上一层提供服务。会话层是第 5 层，其直接下层是第 4 层传输层，因此直接为会话层提供服务的是传输层，选 C。

表示层位于会话层之上，由会话层为其提供服务；应用层位于最上层；网络层位于传输层之下，并不直接与会话层相邻。




</details>

6. 【2016】在 OSI 参考模型中,路由器、交换机 (Switch)、集线器 (Hub) 实现的最高功能层分别是（ ）。

A. 2,2,1  
B. 2,2,2  
C. 3,2,1  
D. 3,2,2  


<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 路由器根据网络层地址进行分组转发和路由选择，其实现的最高功能层是网络层，即第 3 层。

传统以太网交换机根据 MAC 地址转发以太网帧，最高工作在数据链路层，即第 2 层。集线器仅对收到的比特信号进行整形、放大并向其他端口转发，不识别帧和地址，最高工作在物理层，即第 1 层。

因此三种设备对应的最高功能层依次为 $3,2,1$，选 C。




</details>

7. 【2017】假设 OSI 参考模型的应用层欲发送 400B 的数据 (无拆分), 除物理层和应用层之外,其他各层在封装 PDU 时均引入 20B 的额外开销,则应用层数据传输效率约为（ ）。

A. 80%  
B. 83%  
C. 87%  
D. 91%  


<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** OSI 参考模型共有 7 层。题目说明物理层和应用层不引入额外开销，因此引入开销的是表示层、会话层、传输层、网络层和数据链路层，共 5 层。

每层增加 $20\text{B}$，总开销为

$$
\displaystyle 5\times 20\text{B}=100\text{B}.
$$

实际传输的总数据量为

$$
\displaystyle 400\text{B}+100\text{B}=500\text{B}.
$$

应用层数据传输效率为

$$
\displaystyle \eta=\dfrac{400}{500}\times 100\%=80\%.
$$

因此选 A。




</details>

8. 【2019】OSI 参考模型的第 5 层 (自下而上) 完成的主要功能是（ ）。

A. 差错控制  
B. 路由选择  
C. 会话管理  
D. 数据表示转换  


<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** OSI 参考模型自下而上依次为：

1. 物理层；
2. 数据链路层；
3. 网络层；
4. 传输层；
5. 会话层；
6. 表示层；
7. 应用层。

因此第 5 层是会话层，其主要功能是建立、管理和终止会话，并进行对话控制和同步，故选 C。

A 项差错控制可由数据链路层或传输层等实现；B 项路由选择属于网络层；D 项数据表示转换属于表示层。




</details>

9. 【2020】下图描述的协议要素是（ ）。

![第 1 章 计算机网络概述 1.1 第9题：图表，见原 PDF 第5页附近](/img/questions/408/computer-network-1-1-q9.png)

I. 语法 II. 语义 III. 时序

A. 仅 I  
B. 仅 II  
C. 仅 III  
D. I、 II 和 III  


<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 协议通常由语法、语义和时序三个要素组成：

- 语法规定所交换数据与控制信息的格式和结构；
- 语义规定各字段的含义以及通信双方应完成的动作；
- 时序规定事件实现顺序、通信速度匹配等时间关系。

图中以发送方、接收方两条时间轴和多条消息箭头表示通信过程中消息发送、接收及响应的先后顺序，主要描述的是协议的时序要素。图中没有给出报文格式或字段结构，不能体现语法；也没有标明各消息的具体含义和处理动作，不能体现语义。因此仅 III 正确，选 C。




</details>

10. 【2021】在 TCP/IP 参考模型中, 由传输层相邻的下一层实现的主要功能是（ ）

A. 对话管理  
B. 路由选择  
C. 端到端报文段传输  
D. 结点到结点流量控制  


<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** TCP/IP 参考模型中，传输层的相邻下一层是网际层。网际层的主要任务是使用 IP 地址进行分组转发和路由选择，使分组能够跨越多个网络到达目的主机，因此选 B。

A 项对话管理属于 OSI 会话层的功能；C 项端到端报文段传输正是传输层自身的主要功能；D 项相邻结点之间的流量控制通常属于数据链路层功能。




</details>

11. 【2022】在 ISO/OSI 参考模型中, 实现两个相邻结点间流量控制功能的是（ ）。

A. 物理层  
B. 数据链路层  
C. 网络层  
D. 传输层  


<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 数据链路层负责在一条具体链路上的两个相邻结点之间传输帧。为了防止发送结点发送过快、接收结点来不及处理，数据链路层可实施点到点的流量控制，因此选 B。

物理层只负责透明传输比特流；网络层主要负责路由选择和分组转发；传输层负责端系统进程之间的端到端流量控制，而题目强调的是“两个相邻结点间”，故 D 项不正确。


</details>
