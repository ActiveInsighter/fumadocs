---
title: "六、两类函数相乘的积分"
---

## 小题

1. **【1989-5-5 分】** 求不定积分

$$
\displaystyle \int \dfrac{x+\ln(1-x)}{x^2}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle \int \dfrac{x+\ln(1-x)}{x^2}\,dx
=\ln(1-x)-\dfrac{\ln(1-x)}{x}+C.
$$

</div>
</div>

**解析：** 先将积分拆分为

$$
  \displaystyle I=\int\dfrac{dx}{x}+\int\dfrac{\ln(1-x)}{x^2}\,dx.
$$

对第二项使用分部积分，取

$$
  \displaystyle u=\ln(1-x),\qquad dv=\dfrac{dx}{x^2},
$$

则

$$
  \displaystyle du=-\dfrac{dx}{1-x},\qquad v=-\dfrac1x.
$$

因而

$$
  \begin{aligned}
  \displaystyle \int\dfrac{\ln(1-x)}{x^2}\,dx
  &=-\dfrac{\ln(1-x)}x-\int\dfrac{dx}{x(1-x)}\\
  &=-\dfrac{\ln(1-x)}x-\int\left(\dfrac1x+\dfrac1{1-x}\right)dx\\
  &=-\dfrac{\ln(1-x)}x-\ln|x|+\ln(1-x)+C.
  \end{aligned}
$$

再与第一项 $\displaystyle \int\dfrac{dx}{x}=\ln|x|$ 相加，$\ln|x|$ 正好抵消，得到

$$
  \displaystyle I=\ln(1-x)-\dfrac{\ln(1-x)}x+C.
$$

由于原式含有 $\ln(1-x)$，其实数定义域要求 $x<1$，同时还需 $x\ne0$。

</details>

---

2. **【1989-3-4 分】** 求

$$
\displaystyle \int \dfrac{dx}{x\ln^2x}.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle \int \dfrac{dx}{x\ln^2x}=-\dfrac1{\ln x}+C.
$$

</div>
</div>

**解析：** 令

$$
  \displaystyle u=\ln x,
$$

则

$$
  \displaystyle du=\dfrac{dx}{x}.
$$

因此

$$
  \begin{aligned}
  \displaystyle \int\dfrac{dx}{x\ln^2x}
  &=\int u^{-2}\,du\\
  &=-u^{-1}+C\\
  &=-\dfrac1{\ln x}+C.
  \end{aligned}
$$

本题使用的是复合函数积分公式，原被积函数的定义要求 $x>0$ 且 $x\ne1$。

</details>

---

3. **【1990-3-5 分】** 计算

$$
\displaystyle \int \dfrac{\ln x}{(1-x)^2}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle \int \dfrac{\ln x}{(1-x)^2}\,dx
=\dfrac{x\ln x}{1-x}+\ln|1-x|+C.
$$

</div>
</div>

**解析：** 取

$$
  \displaystyle u=\ln x,\qquad dv=\dfrac{dx}{(1-x)^2},
$$

则

$$
  \displaystyle du=\dfrac{dx}{x},\qquad v=\dfrac1{1-x}.
$$

由分部积分公式，

$$
  \begin{aligned}
  \displaystyle I
  &=\dfrac{\ln x}{1-x}-\int\dfrac{dx}{x(1-x)}\\
  &=\dfrac{\ln x}{1-x}-\int\left(\dfrac1x+\dfrac1{1-x}\right)dx\\
  &=\dfrac{\ln x}{1-x}-\ln x+\ln|1-x|+C.
  \end{aligned}
$$

合并前两项：

$$
  \displaystyle \dfrac{\ln x}{1-x}-\ln x
=\dfrac{x\ln x}{1-x},
$$

故

$$
  \displaystyle I=\dfrac{x\ln x}{1-x}+\ln|1-x|+C.
$$

</details>

---

4. **【1990-5-5 分】** 求不定积分

$$
\displaystyle \int \dfrac{x\cos^4\dfrac{x}{2}}{\sin^3x}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle \int \dfrac{x\cos^4\dfrac{x}{2}}{\sin^3x}\,dx
=-\dfrac{x}{8}\cot^2\dfrac{x}{2}-\dfrac14\cot\dfrac{x}{2}-\dfrac{x}{8}+C.
$$

</div>
</div>

**解析：** 利用

$$
  \displaystyle \sin x=2\sin\dfrac{x}{2}\cos\dfrac{x}{2},
$$

有

$$
  \begin{aligned}
  \displaystyle \dfrac{\cos^4\dfrac{x}{2}}{\sin^3x}
  &=\dfrac{\cos^4\dfrac{x}{2}}{8\sin^3\dfrac{x}{2}\cos^3\dfrac{x}{2}}\\
  &=\dfrac18\cot\dfrac{x}{2}\csc^2\dfrac{x}{2}.
  \end{aligned}
$$

又因为

$$
  \displaystyle \dfrac{d}{dx}\left(\cot^2\dfrac{x}{2}\right)
=-\cot\dfrac{x}{2}\csc^2\dfrac{x}{2},
$$

所以

$$
  \displaystyle I=-\dfrac18\int x\,d\left(\cot^2\dfrac{x}{2}\right).
$$

分部积分得

$$
  \displaystyle I=-\dfrac{x}{8}\cot^2\dfrac{x}{2}
+\dfrac18\int\cot^2\dfrac{x}{2}\,dx.
$$

利用 $\displaystyle \cot^2u=\csc^2u-1$，可得

$$
  \begin{aligned}
  \displaystyle \int\cot^2\dfrac{x}{2}\,dx
  &=\int\left(\csc^2\dfrac{x}{2}-1\right)dx\\
  &=-2\cot\dfrac{x}{2}-x+C.
  \end{aligned}
$$

代回即得

$$
  \displaystyle I=-\dfrac{x}{8}\cot^2\dfrac{x}{2}
-\dfrac14\cot\dfrac{x}{2}-\dfrac{x}{8}+C.
$$

</details>

---

5. **【1991-5-5 分】** 求不定积分

$$
\displaystyle I=\int \dfrac{x^2}{1+x^2}\arctan x\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle I=x\arctan x-\dfrac12\ln(1+x^2)
-\dfrac12(\arctan x)^2+C.
$$

</div>
</div>

**解析：** 先作恒等变形

$$
  \displaystyle \dfrac{x^2}{1+x^2}=1-\dfrac1{1+x^2}.
$$

因此

$$
  \displaystyle I=\int\arctan x\,dx
-\int\dfrac{\arctan x}{1+x^2}\,dx.
$$

第一项分部积分：

$$
  \begin{aligned}
  \displaystyle \int\arctan x\,dx
  &=x\arctan x-\int\dfrac{x}{1+x^2}\,dx\\
  &=x\arctan x-\dfrac12\ln(1+x^2).
  \end{aligned}
$$

第二项令 $\displaystyle u=\arctan x$，则 $\displaystyle du=\dfrac{dx}{1+x^2}$，故

$$
  \displaystyle \int\dfrac{\arctan x}{1+x^2}\,dx
=\dfrac12(\arctan x)^2.
$$

合并得到

$$
  \displaystyle I=x\arctan x-\dfrac12\ln(1+x^2)
-\dfrac12(\arctan x)^2+C.
$$

</details>

---

6. **【1991-3-5 分】** 求

$$
\displaystyle \int x\sin^2x\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle \int x\sin^2x\,dx
=\dfrac{x^2}{4}-\dfrac{x\sin2x}{4}-\dfrac{\cos2x}{8}+C.
$$

</div>
</div>

**解析：** 由降幂公式

$$
  \displaystyle \sin^2x=\dfrac{1-\cos2x}{2},
$$

可得

$$
  \displaystyle I=\dfrac12\int x\,dx-\dfrac12\int x\cos2x\,dx.
$$

对第二项分部积分，取

$$
  \displaystyle u=x,\qquad dv=\cos2x\,dx,
$$

则

$$
  \displaystyle du=dx,\qquad v=\dfrac12\sin2x.
$$

因而

$$
  \begin{aligned}
  \displaystyle \int x\cos2x\,dx
  &=\dfrac{x\sin2x}{2}-\dfrac12\int\sin2x\,dx\\
  &=\dfrac{x\sin2x}{2}+\dfrac{\cos2x}{4}.
  \end{aligned}
$$

代回后得到

$$
  \displaystyle I=\dfrac{x^2}{4}-\dfrac{x\sin2x}{4}
-\dfrac{\cos2x}{8}+C.
$$

</details>

---

7. **【1992-45-5 分】** 计算

$$
\displaystyle I=\int \dfrac{\operatorname{arccot}e^x}{e^x}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle I=-e^{-x}\operatorname{arccot}e^x-x
+\dfrac12\ln(1+e^{2x})+C.
$$

</div>
</div>

**解析：** 将原式写成

$$
  \displaystyle I=\int e^{-x}\operatorname{arccot}e^x\,dx.
$$

取

$$
  \displaystyle u=\operatorname{arccot}e^x,
  \qquad dv=e^{-x}dx,
$$

则

$$
  \displaystyle du=-\dfrac{e^x}{1+e^{2x}}dx,
  \qquad v=-e^{-x}.
$$

由分部积分公式，

$$
  \begin{aligned}
  \displaystyle I
  &=-e^{-x}\operatorname{arccot}e^x
-\int\dfrac{dx}{1+e^{2x}}.
  \end{aligned}
$$

对剩余积分作变形：

$$
  \begin{aligned}
  \displaystyle \int\dfrac{dx}{1+e^{2x}}
  &=\int\left(1-\dfrac{e^{2x}}{1+e^{2x}}\right)dx\\
  &=x-\dfrac12\ln(1+e^{2x})+C.
  \end{aligned}
$$

因此

$$
  \displaystyle I=-e^{-x}\operatorname{arccot}e^x-x
+\dfrac12\ln(1+e^{2x})+C.
$$

</details>

---

8. **【1993-12-5 分】** 求

$$
\displaystyle \int \dfrac{xe^x}{\sqrt{e^x-1}}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle \int \dfrac{xe^x}{\sqrt{e^x-1}}\,dx
=2x\sqrt{e^x-1}-4\sqrt{e^x-1}
+4\arctan\sqrt{e^x-1}+C.
$$

</div>
</div>

**解析：** 注意到

$$
  \displaystyle d\sqrt{e^x-1}=\dfrac{e^x}{2\sqrt{e^x-1}}dx,
$$

因此

$$
  \displaystyle \dfrac{e^x}{\sqrt{e^x-1}}dx
=2\,d\sqrt{e^x-1}.
$$

对原积分分部积分，得

$$
  \begin{aligned}
  \displaystyle I
  &=2\int x\,d\sqrt{e^x-1}\\
  &=2x\sqrt{e^x-1}-2\int\sqrt{e^x-1}\,dx.
  \end{aligned}
$$

记

$$
  \displaystyle J=\int\sqrt{e^x-1}\,dx,
$$

令

$$
  \displaystyle t=\sqrt{e^x-1},
$$

则 $\displaystyle e^x=1+t^2$，且

$$
  \displaystyle dx=\dfrac{2t}{1+t^2}dt.
$$

从而

$$
  \begin{aligned}
  \displaystyle J
  &=2\int\dfrac{t^2}{1+t^2}dt\\
  &=2\int\left(1-\dfrac1{1+t^2}\right)dt\\
  &=2t-2\arctan t+C.
  \end{aligned}
$$

代回 $\displaystyle t=\sqrt{e^x-1}$，并代入 $I$，得到

$$
  \displaystyle I=2x\sqrt{e^x-1}-4\sqrt{e^x-1}
+4\arctan\sqrt{e^x-1}+C.
$$

</details>

---

9. **【1993-3-3 分】** 已知曲线 $\displaystyle y=f(x)$ 过点 $\displaystyle \left(0,-\dfrac12\right)$，且其上任一点 $\displaystyle (x,y)$ 处的切线斜率为 $\displaystyle x\ln(1+x^2)$，则 $\displaystyle f(x)=\underline{\qquad}$。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle f(x)=\dfrac{1+x^2}{2}\ln(1+x^2)-\dfrac{1+x^2}{2}.
$$

</div>
</div>

**解析：** 由切线斜率条件可知

$$
  \displaystyle f'(x)=x\ln(1+x^2).
$$

两边积分，令

$$
  \displaystyle u=1+x^2,\qquad du=2x\,dx,
$$

则

$$
  \begin{aligned}
  \displaystyle f(x)
  &=\int x\ln(1+x^2)dx\\
  &=\dfrac12\int\ln u\,du\\
  &=\dfrac12(u\ln u-u)+C\\
  &=\dfrac{1+x^2}{2}\ln(1+x^2)-\dfrac{1+x^2}{2}+C.
  \end{aligned}
$$

利用曲线过点 $\displaystyle \left(0,-\dfrac12\right)$，有

$$
  \displaystyle -\dfrac12=f(0)=-\dfrac12+C,
$$

故 $\displaystyle C=0$，从而

$$
  \displaystyle f(x)=\dfrac{1+x^2}{2}\ln(1+x^2)-\dfrac{1+x^2}{2}.
$$

</details>

---

10. **【1994-3-3 分】**

$$
\displaystyle \int x^3e^{x^2}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle \int x^3e^{x^2}\,dx
=\dfrac12(x^2-1)e^{x^2}+C.
$$

</div>
</div>

**解析：** 令

$$
  \displaystyle u=x^2,
$$

则

$$
  \displaystyle du=2x\,dx,
  \qquad x^3dx=x^2(xdx)=\dfrac{u}{2}du.
$$

因而

$$
  \begin{aligned}
  \displaystyle \int x^3e^{x^2}\,dx
  &=\dfrac12\int ue^u\,du\\
  &=\dfrac12(ue^u-e^u)+C\\
  &=\dfrac12(u-1)e^u+C\\
  &=\dfrac12(x^2-1)e^{x^2}+C.
  \end{aligned}
$$

</details>

---

11. **【1997-2-5 分】** 计算

$$
\displaystyle \int e^{2x}(\tan x+1)^2\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle \int e^{2x}(\tan x+1)^2\,dx=e^{2x}\tan x+C.
$$

</div>
</div>

**解析：** 利用恒等式

$$
  \displaystyle (\tan x+1)^2=\tan^2x+2\tan x+1=\sec^2x+2\tan x,
$$

原积分化为

$$
  \displaystyle \int e^{2x}(\sec^2x+2\tan x)\,dx.
$$

又因为

$$
  \begin{aligned}
  \displaystyle \dfrac{d}{dx}\left(e^{2x}\tan x\right)
  &=2e^{2x}\tan x+e^{2x}\sec^2x\\
  &=e^{2x}(\sec^2x+2\tan x),
  \end{aligned}
$$

所以

$$
  \displaystyle \int e^{2x}(\tan x+1)^2\,dx=e^{2x}\tan x+C.
$$

</details>

---

12. **【1998-34-3 分】**

$$
\displaystyle \int \dfrac{\ln x-1}{x^2}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle -\dfrac{\ln x}{x}+C.
$$

</div>
</div>

**解析：** 由商的求导法则，

$$
  \begin{aligned}
  \displaystyle \left(\dfrac{\ln x}{x}\right)'
  &=\dfrac{1-\ln x}{x^2},
  \end{aligned}
$$

因此

$$
  \displaystyle \dfrac{\ln x-1}{x^2}
=-\left(\dfrac{\ln x}{x}\right)'.
$$

故

$$
  \displaystyle \int \dfrac{\ln x-1}{x^2}\,dx
=-\dfrac{\ln x}{x}+C.
$$

</details>

---

13. **【1998-2-3 分】**

$$
\displaystyle \int \dfrac{\ln\sin x}{\sin^2x}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle -\cot x\ln|\sin x|-\cot x-x+C.
$$

</div>
</div>

**解析：** 在被积函数有定义的区间内，作分部积分。取

$$
  \displaystyle u=\ln|\sin x|,\qquad dv=\csc^2x\,dx,
$$

则

$$
  \displaystyle du=\cot x\,dx,\qquad v=-\cot x.
$$

因而

$$
  \begin{aligned}
  \displaystyle \int \ln|\sin x|\csc^2x\,dx
  &=-\cot x\ln|\sin x|+\int\cot^2x\,dx\\
  &=-\cot x\ln|\sin x|+\int(\csc^2x-1)\,dx\\
  &=-\cot x\ln|\sin x|-\cot x-x+C.
  \end{aligned}
$$

在 $\sin x>0$ 的区间内可写为 $\ln\sin x+C$；写成 $\ln|\sin x|+C$ 可覆盖所有被积函数有定义的区间。

</details>

---

14. **【2000-2-5 分】** 设

$$
\displaystyle f(\ln x)=\dfrac{\ln(1+x)}{x},
$$

计算

$$
\displaystyle \int f(x)\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle \int f(x)\,dx
=x-\left(1+e^{-x}\right)\ln(1+e^x)+C.
$$

</div>
</div>

**解析：** 令 $t=\ln x$，则 $x=e^t$，由已知条件得

$$
  \displaystyle f(t)=e^{-t}\ln(1+e^t).
$$

将自变量 $t$ 改写为 $x$，有

$$
  \displaystyle f(x)=e^{-x}\ln(1+e^x).
$$

于是

$$
  \displaystyle I=\int e^{-x}\ln(1+e^x)\,dx.
$$

分部积分，取

$$
  \displaystyle u=\ln(1+e^x),\qquad dv=e^{-x}\,dx,
$$

则

$$
  \displaystyle du=\dfrac{e^x}{1+e^x}\,dx,\qquad v=-e^{-x}.
$$

因此

$$
  \begin{aligned}
  \displaystyle I
  &=-e^{-x}\ln(1+e^x)+\int\dfrac{1}{1+e^x}\,dx\\
  &=-e^{-x}\ln(1+e^x)+x-\ln(1+e^x)+C\\
  &=x-\left(1+e^{-x}\right)\ln(1+e^x)+C.
  \end{aligned}
$$

</details>

---

15. **【2000-4-3 分】**

$$
\displaystyle \int \dfrac{\arcsin\sqrt{x}}{\sqrt{x}}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle 2\sqrt{x}\arcsin\sqrt{x}+2\sqrt{1-x}+C.
$$

</div>
</div>

**解析：** 令

$$
  \displaystyle t=\sqrt{x},\qquad x=t^2,\qquad dx=2t\,dt,
$$

则

$$
  \displaystyle \int \dfrac{\arcsin\sqrt{x}}{\sqrt{x}}\,dx
=2\int\arcsin t\,dt.
$$

对 $\int\arcsin t\,dt$ 分部积分：

$$
  \begin{aligned}
  \displaystyle \int\arcsin t\,dt
  &=t\arcsin t-\int\dfrac{t}{\sqrt{1-t^2}}\,dt\\
  &=t\arcsin t+\sqrt{1-t^2}+C.
  \end{aligned}
$$

因此

$$
  \begin{aligned}
  \displaystyle \int \dfrac{\arcsin\sqrt{x}}{\sqrt{x}}\,dx
  &=2t\arcsin t+2\sqrt{1-t^2}+C\\
  &=2\sqrt{x}\arcsin\sqrt{x}+2\sqrt{1-x}+C.
  \end{aligned}
$$

</details>

---

16. **【2002-4-3 分】** 已知 $f(x)$ 的一个原函数为 $\ln^2x$，则

$$
\displaystyle \int xf'(x)\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle 2\ln x-\ln^2x+C.
$$

</div>
</div>

**解析：** 因为 $\ln^2x$ 是 $f(x)$ 的一个原函数，所以

$$
  \displaystyle f(x)=\left(\ln^2x\right)'=\dfrac{2\ln x}{x}.
$$

对原积分分部积分，取 $u=x$，$dv=f'(x)dx$，则

$$
  \begin{aligned}
  \displaystyle \int xf'(x)\,dx
  &=xf(x)-\int f(x)\,dx\\
  &=x\cdot\dfrac{2\ln x}{x}-\ln^2x+C\\
  &=2\ln x-\ln^2x+C.
  \end{aligned}
$$

</details>

---

17. **【2018-3-4 分】**

$$
\displaystyle \int e^x\arcsin e^x\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle e^x\arcsin e^x+\sqrt{1-e^{2x}}+C.
$$

</div>
</div>

**解析：** 令

$$
\displaystyle t=e^x,\qquad dt=e^x\,dx,
$$

则

$$
\displaystyle \int e^x\arcsin e^x\,dx=\int\arcsin t\,dt.
$$

对右端分部积分，取

$$
\displaystyle u=\arcsin t,\qquad dv=dt,
$$

则

$$
\displaystyle du=\dfrac{dt}{\sqrt{1-t^2}},\qquad v=t.
$$

因此

$$
\begin{aligned}
\displaystyle \int\arcsin t\,dt
&=t\arcsin t-\int\dfrac{t}{\sqrt{1-t^2}}\,dt\\
&=t\arcsin t+\sqrt{1-t^2}+C.
\end{aligned}
$$

代回 $\displaystyle t=e^x$，得

$$
\displaystyle \int e^x\arcsin e^x\,dx
=e^x\arcsin e^x+\sqrt{1-e^{2x}}+C.
$$

在实数范围内，需有 $\displaystyle e^x\le1$，即 $\displaystyle x\le0$。

</details>

## 大题

1. **【1994-5-6 分】** 已知 $\displaystyle \dfrac{\sin x}{x}$ 是函数 $f(x)$ 的一个原函数，求

$$
\displaystyle \int x^3f'(x)\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle x^2\cos x-4x\sin x-6\cos x+C.
$$

</div>
</div>

**解析：** 因为 $\displaystyle \dfrac{\sin x}{x}$ 是 $f(x)$ 的一个原函数，所以

$$
  \begin{aligned}
  \displaystyle f(x)
  &=\left(\dfrac{\sin x}{x}\right)'\\
  &=\dfrac{x\cos x-\sin x}{x^2}.
  \end{aligned}
$$

对所求积分分部积分：

$$
  \begin{aligned}
  \displaystyle \int x^3f'(x)\,dx
  &=x^3f(x)-\int3x^2f(x)\,dx\\
  &=x\bigl(x\cos x-\sin x\bigr)
-3\int(x\cos x-\sin x)\,dx.
  \end{aligned}
$$

又

$$
  \begin{aligned}
  \displaystyle \int(x\cos x-\sin x)\,dx
  &=\int x\cos x\,dx-\int\sin x\,dx\\
  &=x\sin x+\cos x+\cos x\\
  &=x\sin x+2\cos x.
  \end{aligned}
$$

因而

$$
  \begin{aligned}
  \displaystyle \int x^3f'(x)\,dx
  &=x^2\cos x-x\sin x-3x\sin x-6\cos x+C\\
  &=x^2\cos x-4x\sin x-6\cos x+C.
  \end{aligned}
$$

</details>

---

2. **【1995-5-7 分】** 求不定积分

$$
\displaystyle \int(\arcsin x)^2\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle x(\arcsin x)^2+2\sqrt{1-x^2}\arcsin x-2x+C.
$$

</div>
</div>

**解析：** 设

$$
  \displaystyle I=\int(\arcsin x)^2\,dx.
$$

第一次分部积分，取

$$
  \displaystyle u=(\arcsin x)^2,\qquad dv=dx,
$$

则

$$
  \displaystyle du=\dfrac{2\arcsin x}{\sqrt{1-x^2}}\,dx,
  \qquad v=x.
$$

所以

$$
  \displaystyle I=x(\arcsin x)^2
-2\int\dfrac{x\arcsin x}{\sqrt{1-x^2}}\,dx.
$$

记

$$
  \displaystyle J=\int\dfrac{x\arcsin x}{\sqrt{1-x^2}}\,dx.
$$

注意到

$$
  \displaystyle \dfrac{x}{\sqrt{1-x^2}}\,dx=-d\sqrt{1-x^2},
$$

再次分部积分得

$$
  \begin{aligned}
  \displaystyle J
  &=-\int\arcsin x\,d\sqrt{1-x^2}\\
  &=-\sqrt{1-x^2}\arcsin x
+\int\sqrt{1-x^2}\cdot\dfrac{dx}{\sqrt{1-x^2}}\\
  &=-\sqrt{1-x^2}\arcsin x+x.
  \end{aligned}
$$

故

$$
  \begin{aligned}
  \displaystyle I
  &=x(\arcsin x)^2-2J\\
  &=x(\arcsin x)^2+2\sqrt{1-x^2}\arcsin x-2x+C.
  \end{aligned}
$$

</details>

---

3. **【1996-3-8 分】** 计算不定积分

$$
\displaystyle \int \dfrac{\arctan x}{x^2(1+x^2)}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle -\dfrac{\arctan x}{x}+\ln|x|-\dfrac12\ln(1+x^2)
-\dfrac12(\arctan x)^2+C.
$$

</div>
</div>

**解析：** 先拆分有理因子：

$$
  \displaystyle \dfrac{1}{x^2(1+x^2)}
=\dfrac{1}{x^2}-\dfrac{1}{1+x^2}.
$$

因此

$$
  \displaystyle I
=\int\dfrac{\arctan x}{x^2}\,dx
-\int\dfrac{\arctan x}{1+x^2}\,dx.
$$

对第一项分部积分，取

$$
  \displaystyle u=\arctan x,
  \qquad dv=\dfrac{dx}{x^2},
$$

则

$$
  \displaystyle du=\dfrac{dx}{1+x^2},
  \qquad v=-\dfrac1x.
$$

所以

$$
  \begin{aligned}
  \displaystyle \int\dfrac{\arctan x}{x^2}\,dx
  &=-\dfrac{\arctan x}{x}
+\int\dfrac{dx}{x(1+x^2)}\\
  &=-\dfrac{\arctan x}{x}
+\int\left(\dfrac1x-\dfrac{x}{1+x^2}\right)dx\\
  &=-\dfrac{\arctan x}{x}
+\ln|x|-\dfrac12\ln(1+x^2).
  \end{aligned}
$$

第二项令 $\displaystyle t=\arctan x$，则 $\displaystyle dt=\dfrac{dx}{1+x^2}$，故

$$
  \displaystyle \int\dfrac{\arctan x}{1+x^2}\,dx
=\dfrac12(\arctan x)^2.
$$

综上，

$$
  \displaystyle I
=-\dfrac{\arctan x}{x}+\ln|x|-\dfrac12\ln(1+x^2)
-\dfrac12(\arctan x)^2+C.
$$

</details>

---

4. **【1999-4-6 分】** 设 $F(x)$ 为 $f(x)$ 的原函数，且当 $x\ge0$ 时，

$$
\displaystyle f(x)F(x)=\dfrac{xe^x}{2(1+x)^2},
$$

已知 $\displaystyle F(0)=1$，$\displaystyle F(x)>0$，试求 $f(x)$。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle f(x)=\dfrac{xe^{x/2}}{2(1+x)^{3/2}},\qquad x\ge0.
$$

</div>
</div>

**解析：** 因为 $F(x)$ 是 $f(x)$ 的原函数，所以

$$
  \displaystyle F'(x)=f(x).
$$

由题设，

$$
  \displaystyle F(x)F'(x)=\dfrac{xe^x}{2(1+x)^2},
$$

从而

$$
  \displaystyle \bigl[F^2(x)\bigr]'
=\dfrac{xe^x}{(1+x)^2}.
$$

注意到

$$
  \displaystyle \left(\dfrac{e^x}{1+x}\right)'
=\dfrac{xe^x}{(1+x)^2},
$$

因此

$$
  \displaystyle F^2(x)=\dfrac{e^x}{1+x}+C.
$$

代入 $\displaystyle F(0)=1$，得

$$
  \displaystyle 1=1+C,
$$

故 $\displaystyle C=0$。又因 $\displaystyle F(x)>0$，所以

$$
  \displaystyle F(x)=\sqrt{\dfrac{e^x}{1+x}}
=\dfrac{e^{x/2}}{\sqrt{1+x}}.
$$

最后求导：

$$
  \begin{aligned}
  \displaystyle f(x)=F'(x)
  &=\dfrac{e^{x/2}}{2\sqrt{1+x}}
-\dfrac{e^{x/2}}{2(1+x)^{3/2}}\\
  &=\dfrac{xe^{x/2}}{2(1+x)^{3/2}}.
  \end{aligned}
$$

</details>

---

5. **【2001-1-6 分】** 求

$$
\displaystyle \int \dfrac{\arctan e^x}{e^{2x}}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle -\dfrac{\arctan e^x}{2e^{2x}}-\dfrac{1}{2e^x}
-\dfrac12\arctan e^x+C.
$$

</div>
</div>

**解析：** 令

$$
  \displaystyle t=e^x,\qquad dx=\dfrac{dt}{t},
$$

则

$$
  \displaystyle \int \dfrac{\arctan e^x}{e^{2x}}\,dx
=\int \dfrac{\arctan t}{t^3}\,dt.
$$

对右端作分部积分，取

$$
  \displaystyle u=\arctan t,\qquad dv=\dfrac{dt}{t^3},
$$

则

$$
  \displaystyle du=\dfrac{dt}{1+t^2},\qquad v=-\dfrac{1}{2t^2}.
$$

因而

$$
  \begin{aligned}
  \displaystyle \int \dfrac{\arctan t}{t^3}\,dt
  &=-\dfrac{\arctan t}{2t^2}
+\dfrac12\int\dfrac{dt}{t^2(1+t^2)}.
  \end{aligned}
$$

又

$$
  \displaystyle \dfrac{1}{t^2(1+t^2)}
=\dfrac1{t^2}-\dfrac1{1+t^2},
$$

所以

$$
  \begin{aligned}
  \displaystyle \int \dfrac{\arctan t}{t^3}\,dt
  &=-\dfrac{\arctan t}{2t^2}
+\dfrac12\left(-\dfrac1t-\arctan t\right)+C.
  \end{aligned}
$$

代回 $\displaystyle t=e^x$，得

$$
  \displaystyle \int \dfrac{\arctan e^x}{e^{2x}}\,dx
=-\dfrac{\arctan e^x}{2e^{2x}}-\dfrac{1}{2e^x}
-\dfrac12\arctan e^x+C.
$$

</details>

---

6. **【2002-34-6 分】** 设

$$
\displaystyle f(\sin^2x)=\dfrac{x}{\sin x},
$$

求

$$
\displaystyle \int \dfrac{\sqrt{x}}{\sqrt{1-x}}f(x)\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle 2\sqrt{x}-2\sqrt{1-x}\arcsin\sqrt{x}+C,
\qquad 0<x<1.
$$

</div>
</div>

**解析：** 令

$$
  \displaystyle u=\sin^2t,\qquad 0<t<\dfrac\pi2,
$$

则

$$
  \displaystyle t=\arcsin\sqrt{u},\qquad \sin t=\sqrt{u}.
$$

由题设

$$
  \displaystyle f(u)=\dfrac{t}{\sin t}
=\dfrac{\arcsin\sqrt{u}}{\sqrt{u}}.
$$

因此

$$
  \displaystyle f(x)=\dfrac{\arcsin\sqrt{x}}{\sqrt{x}},
$$

原积分化为

$$
  \displaystyle I=\int\dfrac{\arcsin\sqrt{x}}{\sqrt{1-x}}\,dx.
$$

再令

$$
  \displaystyle \sqrt{x}=\sin t,
$$

则

$$
  \displaystyle x=\sin^2t,\qquad dx=2\sin t\cos t\,dt,
  \qquad \sqrt{1-x}=\cos t.
$$

故

$$
  \begin{aligned}
  \displaystyle I
  &=2\int t\sin t\,dt\\
  &=2\left(-t\cos t+\sin t\right)+C.
  \end{aligned}
$$

代回

$$
  \displaystyle t=\arcsin\sqrt{x},\qquad
  \sin t=\sqrt{x},\qquad \cos t=\sqrt{1-x},
$$

得

$$
  \displaystyle I=2\sqrt{x}-2\sqrt{1-x}\arcsin\sqrt{x}+C.
$$

</details>

---

7. **【2003-2-9 分】** 计算不定积分

$$
\displaystyle \int \dfrac{xe^{\arctan x}}{(1+x^2)^{3/2}}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle \dfrac{(x-1)e^{\arctan x}}{2\sqrt{1+x^2}}+C.
$$

</div>
</div>

**解析：** 令

$$
  \displaystyle t=\arctan x,
$$

则

$$
  \displaystyle x=\tan t,\qquad dx=\sec^2t\,dt,
  \qquad \sqrt{1+x^2}=\sec t.
$$

因而

$$
  \begin{aligned}
  \displaystyle I
  &=\int\dfrac{\tan t\,e^t}{\sec^3t}\sec^2t\,dt\\
  &=\int e^t\sin t\,dt.
  \end{aligned}
$$

利用公式

$$
  \displaystyle \int e^t\sin t\,dt
=\dfrac12e^t(\sin t-\cos t)+C,
$$

且

$$
  \displaystyle \sin t=\dfrac{x}{\sqrt{1+x^2}},
  \qquad \cos t=\dfrac1{\sqrt{1+x^2}},
$$

所以

$$
  \displaystyle I
=\dfrac{(x-1)e^{\arctan x}}{2\sqrt{1+x^2}}+C.
$$

</details>

---

8. **【2006-2-10 分】** 求

$$
\displaystyle \int \dfrac{\arcsin e^x}{e^x}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle -e^{-x}\arcsin e^x
+\ln\left(\dfrac{e^x}{1+\sqrt{1-e^{2x}}}\right)+C,
\qquad x\le0.
$$

</div>
</div>

**解析：** 在实数范围内，需有 $\displaystyle 0<e^x\le1$，即 $\displaystyle x\le0$。

令

$$
  \displaystyle t=e^x,
$$

则

$$
  \displaystyle dx=\dfrac{dt}{t},
$$

从而

$$
  \displaystyle I=\int\dfrac{\arcsin t}{t^2}\,dt.
$$

分部积分，取

$$
  \displaystyle u=\arcsin t,
  \qquad dv=\dfrac{dt}{t^2},
$$

则

$$
  \displaystyle du=\dfrac{dt}{\sqrt{1-t^2}},
  \qquad v=-\dfrac1t.
$$

所以

$$
  \displaystyle I=-\dfrac{\arcsin t}{t}
+\int\dfrac{dt}{t\sqrt{1-t^2}}.
$$

对后一积分令 $\displaystyle t=\sin\theta$，则

$$
  \displaystyle \int\dfrac{dt}{t\sqrt{1-t^2}}
=\int\csc\theta\,d\theta
=\ln\tan\dfrac\theta2+C.
$$

又

$$
  \displaystyle \tan\dfrac\theta2
=\dfrac{\sin\theta}{1+\cos\theta}
=\dfrac{t}{1+\sqrt{1-t^2}},
$$

因此

$$
  \displaystyle I=-\dfrac{\arcsin t}{t}
+\ln\left(\dfrac{t}{1+\sqrt{1-t^2}}\right)+C.
$$

代回 $\displaystyle t=e^x$，即得

$$
  \displaystyle I=-e^{-x}\arcsin e^x
+\ln\left(\dfrac{e^x}{1+\sqrt{1-e^{2x}}}\right)+C.
$$

</details>

---

9. **【2009-23-10 分】** 计算不定积分

$$
\displaystyle \int \ln\left(1+\sqrt{\dfrac{1+x}{x}}\right)\,dx,
\qquad x>0.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\begin{aligned}
\displaystyle &x\ln\left(1+\sqrt{\dfrac{1+x}{x}}\right)
+\dfrac12\ln\left(\sqrt{x}+\sqrt{1+x}\right)\\
&\qquad -\dfrac12\sqrt{x(1+x)}+\dfrac{x}{2}+C.
\end{aligned}
$$

</div>
</div>

**解析：** 由于 $\displaystyle x>0$，有

$$
  \begin{aligned}
  \displaystyle \ln\left(1+\sqrt{\dfrac{1+x}{x}}\right)
  &=\ln\left(\dfrac{\sqrt{x}+\sqrt{1+x}}{\sqrt{x}}\right)\\
  &=\ln\left(\sqrt{x}+\sqrt{1+x}\right)-\dfrac12\ln x.
  \end{aligned}
$$

令

$$
  \displaystyle u=\sqrt{x},\qquad x=u^2,\qquad dx=2u\,du,
$$

则

$$
  \begin{aligned}
  \displaystyle I
  &=2\int u\ln\left(u+\sqrt{1+u^2}\right)du
-2\int u\ln u\,du.
  \end{aligned}
$$

先计算第一项。令

$$
  \displaystyle A=2\int u\ln\left(u+\sqrt{1+u^2}\right)du.
$$

分部积分，取

$$
  \displaystyle U=\ln\left(u+\sqrt{1+u^2}\right),
  \qquad dV=2u\,du,
$$

则

$$
  \displaystyle dU=\dfrac{du}{\sqrt{1+u^2}},
  \qquad V=u^2.
$$

所以

$$
  \begin{aligned}
  \displaystyle A
  &=u^2\ln\left(u+\sqrt{1+u^2}\right)
-\int\dfrac{u^2}{\sqrt{1+u^2}}\,du\\
  &=u^2\ln\left(u+\sqrt{1+u^2}\right)
-\dfrac12u\sqrt{1+u^2}
+\dfrac12\ln\left(u+\sqrt{1+u^2}\right).
  \end{aligned}
$$

其中用到了

$$
  \displaystyle \int\dfrac{u^2}{\sqrt{1+u^2}}\,du
=\dfrac12u\sqrt{1+u^2}
-\dfrac12\ln\left(u+\sqrt{1+u^2}\right).
$$

另一方面，

$$
  \displaystyle 2\int u\ln u\,du
=u^2\ln u-\dfrac{u^2}{2}.
$$

因此

$$
  \begin{aligned}
  \displaystyle I
  &=u^2\ln\left(\dfrac{u+\sqrt{1+u^2}}{u}\right)
+\dfrac12\ln\left(u+\sqrt{1+u^2}\right)\\
  &\qquad -\dfrac12u\sqrt{1+u^2}+\dfrac{u^2}{2}+C.
  \end{aligned}
$$

代回 $\displaystyle u=\sqrt{x}$，得

$$
  \begin{aligned}
  \displaystyle I
  &=x\ln\left(1+\sqrt{\dfrac{1+x}{x}}\right)
+\dfrac12\ln\left(\sqrt{x}+\sqrt{1+x}\right)\\
  &\qquad -\dfrac12\sqrt{x(1+x)}+\dfrac{x}{2}+C.
  \end{aligned}
$$

</details>

---

10. **【2011-3-10 分】** 求不定积分

$$
\displaystyle \int \dfrac{\arcsin \sqrt{x}+\ln x}{\sqrt{x}}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\begin{aligned}
\displaystyle \int \dfrac{\arcsin \sqrt{x}+\ln x}{\sqrt{x}}\,dx
&=2\sqrt{x}\arcsin\sqrt{x}+2\sqrt{1-x}\\
&\quad+2\sqrt{x}\ln x-4\sqrt{x}+C,
\qquad 0<x\le1.
\end{aligned}
$$

</div>
</div>

**解析：** 在实数范围内，由 $\displaystyle \ln x$ 和 $\displaystyle \arcsin\sqrt{x}$ 的定义可知

$$
  \displaystyle 0<x\le1.
$$

令

$$
  \displaystyle t=\sqrt{x},
$$

则

$$
  \displaystyle x=t^2,\qquad dx=2t\,dt,
  \qquad \ln x=2\ln t.
$$

原积分化为

$$
  \begin{aligned}
  \displaystyle I
  &=\int\dfrac{\arcsin t+2\ln t}{t}\cdot2t\,dt\\
  &=2\int\arcsin t\,dt+4\int\ln t\,dt.
  \end{aligned}
$$

对第一项分部积分：

$$
  \begin{aligned}
  \displaystyle \int\arcsin t\,dt
  &=t\arcsin t-\int\dfrac{t}{\sqrt{1-t^2}}\,dt\\
  &=t\arcsin t+\sqrt{1-t^2}.
  \end{aligned}
$$

又

$$
  \displaystyle \int\ln t\,dt=t\ln t-t.
$$

因此

$$
  \begin{aligned}
  \displaystyle I
  &=2t\arcsin t+2\sqrt{1-t^2}
+4t\ln t-4t+C.
  \end{aligned}
$$

代回 $\displaystyle t=\sqrt{x}$，并利用 $\displaystyle \ln\sqrt{x}=\dfrac12\ln x$，得到

$$
  \begin{aligned}
  \displaystyle I
  &=2\sqrt{x}\arcsin\sqrt{x}+2\sqrt{1-x}\\
  &\quad+2\sqrt{x}\ln x-4\sqrt{x}+C.
  \end{aligned}
$$

</details>

---

11. **【2018-12-10 分】** 求不定积分

$$
\displaystyle \int e^{2x}\arctan\sqrt{e^x-1}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle \dfrac12e^{2x}\arctan\sqrt{e^x-1}
-\dfrac{e^x+2}{6}\sqrt{e^x-1}+C,
\qquad x\ge0.
$$

</div>
</div>

**解析：** 在实数范围内，需有 $\displaystyle e^x-1\ge0$，即 $\displaystyle x\ge0$。

令

$$
  \displaystyle t=\sqrt{e^x-1},
$$

则

$$
  \displaystyle e^x=1+t^2,
  \qquad dx=\dfrac{2t}{1+t^2}\,dt,
  \qquad e^{2x}=(1+t^2)^2.
$$

因此

$$
  \begin{aligned}
  \displaystyle I
  &=\int(1+t^2)^2\arctan t\cdot\dfrac{2t}{1+t^2}\,dt\\
  &=\int2t(1+t^2)\arctan t\,dt.
  \end{aligned}
$$

分部积分，取

$$
  \displaystyle u=\arctan t,
  \qquad dv=2t(1+t^2)\,dt,
$$

则

$$
  \displaystyle du=\dfrac{dt}{1+t^2},
  \qquad v=t^2+\dfrac{t^4}{2}.
$$

从而

$$
  \begin{aligned}
  \displaystyle I
  &=\left(t^2+\dfrac{t^4}{2}\right)\arctan t
-\int\dfrac{t^2+\dfrac12t^4}{1+t^2}\,dt.
  \end{aligned}
$$

注意到

$$
  \begin{aligned}
  \displaystyle \dfrac{t^2+\dfrac12t^4}{1+t^2}
  &=\dfrac12\cdot\dfrac{t^4+2t^2}{1+t^2}\\
  &=\dfrac12\left(t^2+1-\dfrac1{1+t^2}\right),
  \end{aligned}
$$

故

$$
  \begin{aligned}
  \displaystyle I
  &=\left(t^2+\dfrac{t^4}{2}\right)\arctan t
-\dfrac{t^3}{6}-\dfrac t2+\dfrac12\arctan t+C\\
  &=\dfrac{(1+t^2)^2}{2}\arctan t
-\dfrac{t^3}{6}-\dfrac t2+C.
  \end{aligned}
$$

代回 $\displaystyle t=\sqrt{e^x-1}$，有

$$
  \displaystyle \dfrac{(1+t^2)^2}{2}=\dfrac12e^{2x},
$$

且

$$
  \begin{aligned}
  \displaystyle \dfrac{t^3}{6}+\dfrac t2
  &=\dfrac{t(t^2+3)}{6}\\
  &=\dfrac{e^x+2}{6}\sqrt{e^x-1}.
  \end{aligned}
$$

所以

$$
  \displaystyle I=\dfrac12e^{2x}\arctan\sqrt{e^x-1}
-\dfrac{e^x+2}{6}\sqrt{e^x-1}+C.
$$

</details>
