---
title: "4.2 二维随机变量函数的分布"
---

## 小题

1. **【1994-1-3 分】** 设相互独立的随机变量 $X,Y$ 具有同一分布律，且 $X$ 的分布律为

| $X$ | $0$ | $1$ |
| --- | --- | --- |
| $P$ | $\dfrac12$ | $\dfrac12$ |

则随机变量 $Z=\max\{X,Y\}$ 的分布律为

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

| $Z$ | $0$ | $1$ |
| --- | --- | --- |
| $P$ | $\dfrac14$ | $\dfrac34$ |

</div>
</div>

**解析：** $Z=0$ 当且仅当 $X=0$ 且 $Y=0$。由独立性，

$$
\displaystyle
P\{Z=0\}
=P\{X=0,Y=0\}
=\dfrac12\cdot\dfrac12
=\dfrac14.
$$

其余情况下至少有一个随机变量等于 $1$，故 $Z=1$，因此

$$
\displaystyle P\{Z=1\}=1-\dfrac14=\dfrac34.
$$

</details>

---

2. **【2008-134-4 分】** 设随机变量 $X,Y$ 独立同分布，且 $X$ 的分布函数为 $F(x)$，则 $Z=\max\{X,Y\}$ 的分布函数为（  ）

<div className="choice-options">
<div className="choice-option-row">A. $F^2(x)$ &emsp;&emsp; B. $F(x)F(y)$ &emsp;&emsp; C. $1-[1-F(x)]^2$ &emsp;&emsp; D. $[1-F(x)][1-F(y)]$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A。

**解析：** 由最大值不超过 $z$ 的充要条件，

$$
\displaystyle
\{Z\le z\}=\{X\le z,\ Y\le z\}.
$$

由于 $X,Y$ 相互独立且同分布，

$$
\displaystyle
F_Z(z)
=P\{X\le z,Y\le z\}
=P\{X\le z\}P\{Y\le z\}
=F^2(z).
$$

因此选 A。

选项 C 是最小值 $Z=\min\{X,Y\}$ 的分布函数；B、D 中混入了无关变量 $y$，不是关于 $z$ 的一元分布函数。

</details>

## 大题

1. **【1994-4-8 分】** 假设随机变量 $X_1,X_2,X_3,X_4$ 相互独立且同分布，

$$
\displaystyle P\{X_i=0\}=0.6,\qquad P\{X_i=1\}=0.4
\quad(i=1,2,3,4).
$$

求行列式

$$
\displaystyle
X=
\begin{vmatrix}
X_1&X_2\\
X_3&X_4
\end{vmatrix}
=X_1X_4-X_2X_3
$$

的概率分布。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

| $X$ | $-1$ | $0$ | $1$ |
| --- | --- | --- | --- |
| $P$ | $0.1344$ | $0.7312$ | $0.1344$ |

</div>
</div>

**解析：** 由于各 $X_i$ 只取 $0,1$，故两个乘积 $X_1X_4$ 与 $X_2X_3$ 也只取 $0,1$，因此行列式只可能取 $-1,0,1$。

记 $p=0.4$，则

$$
\displaystyle P\{X_1X_4=1\}=p^2=0.16,
$$

而

$$
\displaystyle P\{X_2X_3=0\}=1-p^2=0.84.
$$

两组变量相互独立，所以

$$
\displaystyle
P\{X=1\}
=P\{X_1X_4=1,\ X_2X_3=0\}
=0.16\times0.84
=0.1344.
$$

同理，

$$
\displaystyle
P\{X=-1\}
=P\{X_1X_4=0,\ X_2X_3=1\}
=0.84\times0.16
=0.1344.
$$

最后

$$
\displaystyle
P\{X=0\}
=1-0.1344-0.1344
=0.7312.
$$

</details>

---

2. **【2006-4-8 分】** 设二维随机变量 $(X,Y)$ 的概率分布为

| $X\backslash Y$ | $-1$ | $0$ | $1$ |
| --- | --- | --- | --- |
| $-1$ | $a$ | $0$ | $0.2$ |
| $0$ | $0.1$ | $b$ | $0.2$ |
| $1$ | $0$ | $0.1$ | $c$ |

其中 $a,b,c$ 为常数，且

$$
\displaystyle E(X)=-0.2,
\qquad
P\{Y\le0\mid X\le0\}=0.5.
$$

记 $Z=X+Y$，求：

（1）$a,b,c$ 的值；

（2）$Z$ 的概率分布；

（3）$P\{X=Z\}$。

<details>
<summary>查看答案与解析</summary>

**答案：** （1）

$$
\displaystyle a=0.2,\qquad b=0.1,\qquad c=0.1.
$$

（2）

| $Z$ | $-2$ | $-1$ | $0$ | $1$ | $2$ |
| --- | --- | --- | --- | --- | --- |
| $P$ | $0.2$ | $0.1$ | $0.3$ | $0.3$ | $0.1$ |

（3）

$$
\displaystyle P\{X=Z\}=0.2.
$$

**解析：** （1）由所有概率之和为 $1$，

$$
\displaystyle a+b+c+0.6=1,
$$

即

$$
\displaystyle a+b+c=0.4. \tag{1}
$$

计算 $X$ 的边缘概率：

$$
\displaystyle
P\{X=-1\}=a+0.2,
\qquad
P\{X=1\}=0.1+c.
$$

所以

$$
\displaystyle
E(X)=-(a+0.2)+(0.1+c)=c-a-0.1.
$$

由 $E(X)=-0.2$ 得

$$
\displaystyle c-a=-0.1. \tag{2}
$$

再利用条件概率：

$$
\displaystyle
P\{Y\le0,\ X\le0\}=a+0.1+b,
$$

$$
\displaystyle
P\{X\le0\}=(a+0.2)+(0.1+b+0.2)=a+b+0.5.
$$

故

$$
\displaystyle
\dfrac{a+b+0.1}{a+b+0.5}=0.5,
$$

解得

$$
\displaystyle a+b=0.3. \tag{3}
$$

由 (1) 得 $c=0.1$，再由 (2) 得 $a=0.2$，于是 $b=0.1$。

（2）逐项计算 $Z=X+Y$：

- $Z=-2$：由 $(-1,-1)$ 产生，概率为 $0.2$；
- $Z=-1$：由 $(0,-1)$ 产生，概率为 $0.1$；
- $Z=0$：由 $(-1,1)$、$(0,0)$ 产生，概率为 $0.2+0.1=0.3$；
- $Z=1$：由 $(0,1)$、$(1,0)$ 产生，概率为 $0.2+0.1=0.3$；
- $Z=2$：由 $(1,1)$ 产生，概率为 $0.1$。

（3）因为 $Z=X+Y$，所以

$$
\displaystyle X=Z\iff Y=0.
$$

因此

$$
\displaystyle
P\{X=Z\}=P\{Y=0\}=0+b+0.1=0.2.
$$

</details>

---

3. **【2018-13-11 分】** 设随机变量 $X$ 与 $Y$ 相互独立，$X$ 的概率分布为

$$
\displaystyle P\{X=1\}=P\{X=-1\}=\dfrac12.
$$

$Y$ 服从参数为 $\lambda$ 的泊松分布。令 $Z=XY$。

（Ⅰ）求 $\operatorname{Cov}(X,Z)$；

（Ⅱ）求 $Z$ 的概率分布。

<details>
<summary>查看答案与解析</summary>

**答案：** （Ⅰ）

$$
\displaystyle \operatorname{Cov}(X,Z)=\lambda.
$$

（Ⅱ）

$$
\displaystyle P\{Z=0\}=e^{-\lambda},
$$

且对任意正整数 $k$，

$$
\displaystyle
P\{Z=k\}=P\{Z=-k\}
=\dfrac12e^{-\lambda}\dfrac{\lambda^k}{k!}.
$$

**解析：** （Ⅰ）由 $Z=XY$，有

$$
\displaystyle XZ=X^2Y=Y,
$$

因为 $X^2=1$。又

$$
\displaystyle E(X)=\dfrac12-\dfrac12=0.
$$

由于 $X,Y$ 独立，

$$
\displaystyle E(Z)=E(XY)=E(X)E(Y)=0.
$$

因此

$$
\displaystyle
\begin{aligned}
\operatorname{Cov}(X,Z)
&=E(XZ)-E(X)E(Z)\\
&=E(Y)-0\\
&=\lambda.
\end{aligned}
$$

（Ⅱ）当 $Y=0$ 时，无论 $X$ 取何值，均有 $Z=0$，故

$$
\displaystyle P\{Z=0\}=P\{Y=0\}=e^{-\lambda}.
$$

当 $k\ge1$ 时，

$$
\displaystyle
\{Z=k\}=\{X=1,Y=k\},
$$

所以由独立性，

$$
\displaystyle
P\{Z=k\}
=P\{X=1\}P\{Y=k\}
=\dfrac12e^{-\lambda}\dfrac{\lambda^k}{k!}.
$$

类似地，

$$
\displaystyle
\{Z=-k\}=\{X=-1,Y=k\},
$$

从而

$$
\displaystyle
P\{Z=-k\}
=\dfrac12e^{-\lambda}\dfrac{\lambda^k}{k!}.
$$

</details>

---

4. **【1987-1-6 分】** 设随机变量 $X,Y$ 相互独立，其概率密度函数分别为

$$
\displaystyle
f_X(x)=
\begin{cases}
1,&0\le x\le1,\\
0,&\text{其他},
\end{cases}
\qquad
f_Y(y)=
\begin{cases}
e^{-y},&y>0,\\
0,&y\le0.
\end{cases}
$$

求随机变量 $Z=2X+Y$ 的概率密度函数。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle
f_Z(z)=
\begin{cases}
0,&z\le0,\\[1mm]
\dfrac12(1-e^{-z}),&0<z<2,\\[2mm]
\dfrac12(e^2-1)e^{-z},&z\ge2.
\end{cases}
$$

</div>
</div>

**解析：** 令

$$
\displaystyle U=2X.
$$

由于 $X\sim U(0,1)$，故 $U\sim U(0,2)$，其密度为

$$
\displaystyle
f_U(u)=
\begin{cases}
\dfrac12,&0<u<2,\\
0,&\text{其他}.
\end{cases}
$$

又 $U$ 与 $Y$ 独立，且 $Z=U+Y$。由卷积公式，

$$
\displaystyle
f_Z(z)=\int_{-\infty}^{+\infty}f_U(u)f_Y(z-u)\,du.
$$

要使被积函数非零，需同时满足

$$
\displaystyle 0<u<2,\qquad z-u>0,
$$

即

$$
\displaystyle 0<u<\min\{2,z\}.
$$

当 $z\le0$ 时，无可行区间，故 $f_Z(z)=0$。

当 $0<z<2$ 时，

$$
\displaystyle
\begin{aligned}
f_Z(z)
&=\int_0^z\dfrac12e^{-(z-u)}\,du\\
&=\dfrac12e^{-z}\int_0^ze^u\,du\\
&=\dfrac12(1-e^{-z}).
\end{aligned}
$$

当 $z\ge2$ 时，

$$
\displaystyle
\begin{aligned}
f_Z(z)
&=\int_0^2\dfrac12e^{-(z-u)}\,du\\
&=\dfrac12e^{-z}(e^2-1).
\end{aligned}
$$

</details>

---

5. **【1989-1-6 分】** 设随机变量 $X$ 与 $Y$ 独立，且 $X$ 服从均值为 $1$、标准差为 $\sqrt2$ 的正态分布，而 $Y$ 服从标准正态分布。试求随机变量 $Z=2X-Y+3$ 的概率密度函数。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle
f_Z(z)=\dfrac{1}{3\sqrt{2\pi}}
\exp\left[-\dfrac{(z-5)^2}{18}\right],
\quad -\infty<z<+\infty.
$$

</div>
</div>

**解析：** 已知

$$
\displaystyle X\sim N(1,2),\qquad Y\sim N(0,1),
$$

其中 $2$ 和 $1$ 分别为方差。

独立正态随机变量的线性组合仍服从正态分布。对

$$
\displaystyle Z=2X-Y+3,
$$

其均值为

$$
\displaystyle
E(Z)=2E(X)-E(Y)+3=2\cdot1-0+3=5.
$$

方差为

$$
\displaystyle
D(Z)=4D(X)+D(Y)=4\cdot2+1=9,
$$

因为 $X,Y$ 独立，协方差项为 $0$。

所以

$$
\displaystyle Z\sim N(5,9).
$$

因此其密度为

$$
\displaystyle
f_Z(z)
=\dfrac{1}{\sqrt{2\pi\cdot9}}
\exp\left[-\dfrac{(z-5)^2}{2\cdot9}\right]
=\dfrac{1}{3\sqrt{2\pi}}
\exp\left[-\dfrac{(z-5)^2}{18}\right].
$$

</details>

---

6. **【1991-6 分】** 设二维随机变量 $(X,Y)$ 的概率密度为

$$
\displaystyle
f(x,y)=
\begin{cases}
2e^{-(x+2y)},&x>0,\ y>0,\\
0,&\text{其他},
\end{cases}
$$

求 $Z=X+2Y$ 的分布函数。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle
F_Z(z)=
\begin{cases}
0,&z\le0,\\[1mm]
1-(1+z)e^{-z},&z>0.
\end{cases}
$$

</div>
</div>

**解析：** 联合密度可分解为

$$
\displaystyle
f(x,y)=e^{-x}\cdot2e^{-2y},
\qquad x>0,\ y>0.
$$

因此 $X,Y$ 相互独立，且

$$
\displaystyle X\sim \operatorname{Exp}(1),
\qquad
Y\sim \operatorname{Exp}(2).
$$

令

$$
\displaystyle W=2Y.
$$

对 $w>0$，

$$
\displaystyle
f_W(w)
=f_Y\left(\dfrac w2\right)\dfrac12
=2e^{-w}\cdot\dfrac12
=e^{-w}.
$$

故 $W\sim\operatorname{Exp}(1)$，并且 $X,W$ 独立。于是

$$
\displaystyle Z=X+W
$$

是两个相互独立的参数为 $1$ 的指数随机变量之和，服从形状参数为 $2$、率参数为 $1$ 的伽马分布，其密度为

$$
\displaystyle f_Z(z)=ze^{-z},\quad z>0.
$$

所以对 $z>0$，

$$
\displaystyle
\begin{aligned}
F_Z(z)
&=\int_0^zt e^{-t}\,dt\\
&=\left[-(t+1)e^{-t}\right]_0^z\\
&=1-(1+z)e^{-z}.
\end{aligned}
$$

</details>

---

7. **【1999-4-9 分】** 设二维随机变量 $(X,Y)$ 在矩形

$$
\displaystyle G=\{(x,y)\mid0\le x\le2,\ 0\le y\le1\}
$$

上服从均匀分布。试求边长为 $X$ 和 $Y$ 的矩形面积 $S=XY$ 的概率密度 $f_S(s)$。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle
f_S(s)=
\begin{cases}
\dfrac12\ln\dfrac{2}{s},&0<s<2,\\[2mm]
0,&\text{其他}.
\end{cases}
$$

</div>
</div>

**解析：** 矩形 $G$ 的面积为 $2$，所以联合密度为

$$
\displaystyle
f_{X,Y}(x,y)=
\begin{cases}
\dfrac12,&0<x<2,\ 0<y<1,\\
0,&\text{其他}.
\end{cases}
$$

且 $X,Y$ 相互独立。

对乘积 $S=XY$，使用变换密度公式：

$$
\displaystyle
f_S(s)
=\int_{-\infty}^{+\infty}
f_{X,Y}\left(x,\dfrac{s}{x}\right)\dfrac{1}{|x|}\,dx.
$$

由于 $x>0$，要使

$$
\displaystyle 0<\dfrac{s}{x}<1,\qquad 0<x<2,
$$

必须有

$$
\displaystyle 0<s<2,\qquad s<x<2.
$$

故当 $0<s<2$ 时，

$$
\displaystyle
\begin{aligned}
f_S(s)
&=\int_s^2\dfrac12\cdot\dfrac1x\,dx\\
&=\dfrac12\left[\ln x\right]_s^2\\
&=\dfrac12\ln\dfrac2s.
\end{aligned}
$$

其他范围内密度为 $0$。

</details>

---

8. **【2001-3-8 分】** 设随机变量 $X$ 和 $Y$ 的联合分布是正方形

$$
\displaystyle G=\{(x,y)\mid1\le x\le3,\ 1\le y\le3\}
$$

上的均匀分布。试求随机变量 $U=|X-Y|$ 的概率密度 $p(u)$。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle
p(u)=
\begin{cases}
1-\dfrac u2,&0<u<2,\\[2mm]
0,&\text{其他}.
\end{cases}
$$

</div>
</div>

**解析：** 正方形边长为 $2$、面积为 $4$，故联合密度为 $\dfrac14$。

先令

$$
\displaystyle D=X-Y.
$$

由于 $X,Y$ 独立且均服从 $U(1,3)$，差 $D$ 的密度为三角形密度：

$$
\displaystyle
f_D(d)=
\begin{cases}
\dfrac{2-|d|}{4},&|d|<2,\\
0,&\text{其他}.
\end{cases}
$$

又 $U=|D|$。对 $0<u<2$，

$$
\displaystyle
p(u)=f_D(u)+f_D(-u)
=2\cdot\dfrac{2-u}{4}
=1-\dfrac u2.
$$

也可用几何法：$P\{|X-Y|\le u\}$ 对应正方形中对角线两侧宽度为 $u$ 的带状区域，求导后得到同一结果。

</details>

---

9. **【2005-34-13 分】** 设二维随机变量 $(X,Y)$ 的概率密度为

$$
\displaystyle
f(x,y)=
\begin{cases}
1,&0<x<1,\ 0<y<2x,\\
0,&\text{其他}.
\end{cases}
$$

求：

（1）$(X,Y)$ 的边缘概率密度 $f_X(x),f_Y(y)$；

（2）$Z=2X-Y$ 的概率密度 $f_Z(z)$；

（3）$P\left\{Y\le\dfrac12\mid X\le\dfrac12\right\}$。

<details>
<summary>查看答案与解析</summary>

**答案：** （1）

$$
\displaystyle
f_X(x)=
\begin{cases}
2x,&0<x<1,\\
0,&\text{其他},
\end{cases}
$$

$$
\displaystyle
f_Y(y)=
\begin{cases}
1-\dfrac y2,&0<y<2,\\
0,&\text{其他}.
\end{cases}
$$

（2）

$$
\displaystyle
f_Z(z)=
\begin{cases}
1-\dfrac z2,&0<z<2,\\
0,&\text{其他}.
\end{cases}
$$

（3）

$$
\displaystyle
P\left\{Y\le\dfrac12\mid X\le\dfrac12\right\}
=\dfrac34.
$$

**解析：** （1）对固定的 $x$，$y$ 的取值范围为 $0<y<2x$，所以

$$
\displaystyle
f_X(x)=\int_0^{2x}1\,dy=2x,
\qquad 0<x<1.
$$

对固定的 $y$，由 $0<y<2x$ 得 $x>\dfrac y2$，结合 $x<1$，可知 $0<y<2$ 且

$$
\displaystyle \dfrac y2<x<1.
$$

因此

$$
\displaystyle
f_Y(y)=\int_{y/2}^{1}1\,dx
=1-\dfrac y2,
\qquad 0<y<2.
$$

（2）令

$$
\displaystyle z=2x-y,
\qquad y=2x-z.
$$

雅可比绝对值为 $1$。原区域条件

$$
\displaystyle 0<x<1,\qquad 0<2x-z<2x
$$

等价于

$$
\displaystyle z>0,\qquad x>\dfrac z2,\qquad x<1.
$$

因此只有当 $0<z<2$ 时有可行区间，且

$$
\displaystyle
f_Z(z)=\int_{z/2}^{1}1\,dx
=1-\dfrac z2.
$$

（3）先求分母：

$$
\displaystyle
P\left\{X\le\dfrac12\right\}
=\int_0^{1/2}2x\,dx
=\dfrac14.
$$

分子所对应区域为

$$
\displaystyle 0<x\le\dfrac12,
\qquad
0<y\le\min\left\{2x,\dfrac12\right\}.
$$

当 $0<x\le\dfrac14$ 时，上界为 $2x$；当 $\dfrac14<x\le\dfrac12$ 时，上界为 $\dfrac12$。故

$$
\displaystyle
\begin{aligned}
P\left\{Y\le\dfrac12,\ X\le\dfrac12\right\}
&=\int_0^{1/4}2x\,dx
+\int_{1/4}^{1/2}\dfrac12\,dx\\
&=\dfrac1{16}+\dfrac18
=\dfrac3{16}.
\end{aligned}
$$

所以

$$
\displaystyle
P\left\{Y\le\dfrac12\mid X\le\dfrac12\right\}
=\dfrac{3/16}{1/4}
=\dfrac34.
$$

</details>

---

10. **【2005-1-9 分】** 设二维随机变量 $(X,Y)$ 的概率密度为

$$
\displaystyle
f(x,y)=
\begin{cases}
1,&0<x<1,\ 0<y<2x,\\
0,&\text{其他}.
\end{cases}
$$

求：

（1）$(X,Y)$ 的边缘概率密度 $f_X(x),f_Y(y)$；

（2）$Z=2X-Y$ 的概率密度 $f_Z(z)$。

<details>
<summary>查看答案与解析</summary>

**答案：** （1）

$$
\displaystyle
f_X(x)=
\begin{cases}
2x,&0<x<1,\\
0,&\text{其他},
\end{cases}
\qquad
f_Y(y)=
\begin{cases}
1-\dfrac y2,&0<y<2,\\
0,&\text{其他}.
\end{cases}
$$

（2）

$$
\displaystyle
f_Z(z)=
\begin{cases}
1-\dfrac z2,&0<z<2,\\
0,&\text{其他}.
\end{cases}
$$

**解析：** 本题前两问与上一题完全相同。

对 $X$ 的边缘密度，

$$
\displaystyle
f_X(x)=\int_0^{2x}1\,dy=2x,
\qquad 0<x<1.
$$

对 $Y$ 的边缘密度，由 $0<y<2x$ 得 $\dfrac y2<x<1$，所以

$$
\displaystyle
f_Y(y)=\int_{y/2}^{1}1\,dx
=1-\dfrac y2,
\qquad 0<y<2.
$$

令 $Z=2X-Y$，即 $y=2x-z$。原区域条件转化为

$$
\displaystyle 0<z<2,\qquad \dfrac z2<x<1.
$$

因此

$$
\displaystyle
f_Z(z)=\int_{z/2}^{1}1\,dx
=1-\dfrac z2,
\qquad 0<z<2.
$$

</details>

---

11. **【2023-1-12 分】** 设二维随机变量 $(X,Y)$ 的概率密度为

$$
\displaystyle
f(x,y)=
\begin{cases}
\dfrac{2(x^2+y^2)}{\pi},&x^2+y^2\le1,\\
0,&\text{其他}.
\end{cases}
$$

（1）求 $X$ 与 $Y$ 的协方差；

（2）判断 $X$ 与 $Y$ 是否相互独立；

（3）求 $Z=X^2+Y^2$ 的概率密度。

<details>
<summary>查看答案与解析</summary>

**答案：** （1）

$$
\displaystyle \operatorname{Cov}(X,Y)=0.
$$

（2）$X$ 与 $Y$ 不相互独立。

（3）

$$
\displaystyle
f_Z(z)=
\begin{cases}
2z,&0<z<1,\\
0,&\text{其他}.
\end{cases}
$$

**解析：** （1）联合密度关于 $x$ 轴、$y$ 轴均对称，因此

$$
\displaystyle E(X)=E(Y)=0.
$$

同时，函数

$$
\displaystyle xy\,f(x,y)
$$

关于 $x$ 或 $y$ 是奇函数，而积分区域单位圆盘关于相应坐标轴对称，故

$$
\displaystyle E(XY)=0.
$$

因此

$$
\displaystyle
\operatorname{Cov}(X,Y)
=E(XY)-E(X)E(Y)=0.
$$

（2）协方差为 $0$ 只说明 $X,Y$ 不相关，不能推出独立。

联合密度的支持集为圆盘

$$
\displaystyle x^2+y^2\le1,
$$

并不是两个边缘支持区间的直积。例如 $X,Y$ 的边缘密度在 $x=0.9$、$y=0.9$ 附近均为正，但点 $(0.9,0.9)$ 位于单位圆盘之外，联合密度为 $0$。因此联合密度不可能分解为 $f_X(x)f_Y(y)$，故 $X,Y$ 不独立。

（3）令极坐标

$$
\displaystyle x=r\cos\theta,\qquad y=r\sin\theta.
$$

则

$$
\displaystyle Z=r^2.
$$

对 $0<z<1$，

$$
\displaystyle
\begin{aligned}
F_Z(z)
&=P\{X^2+Y^2\le z\}\\
&=\int_0^{2\pi}\int_0^{\sqrt z}
\dfrac{2r^2}{\pi}\,r\,dr\,d\theta\\
&=\dfrac{2}{\pi}\cdot2\pi
\int_0^{\sqrt z}r^3\,dr\\
&=4\cdot\dfrac{z^2}{4}
=z^2.
\end{aligned}
$$

故

$$
\displaystyle f_Z(z)=F_Z'(z)=2z,
\qquad 0<z<1.
$$

</details>

---

12. **【1992-1-6 分】** 设随机变量 $X$ 与 $Y$ 独立，$X$ 服从正态分布 $N(\mu,\sigma^2)$，$Y$ 服从 $[-\pi,\pi]$ 上的均匀分布。试求 $Z=X+Y$ 的概率密度函数。（计算结果用标准正态分布函数 $\Phi(x)$ 表示，其中

$$
\displaystyle
\Phi(x)=\dfrac{1}{\sqrt{2\pi}}
\int_{-\infty}^{x}e^{-t^2/2}\,dt.
$$

）

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle
f_Z(z)
=\dfrac{1}{2\pi}
\left[
\Phi\left(\dfrac{z+\pi-\mu}{\sigma}\right)
-\Phi\left(\dfrac{z-\pi-\mu}{\sigma}\right)
\right],
\quad -\infty<z<+\infty.
$$

</div>
</div>

**解析：** $X$ 的密度为

$$
\displaystyle
f_X(x)
=\dfrac{1}{\sigma\sqrt{2\pi}}
\exp\left[-\dfrac{(x-\mu)^2}{2\sigma^2}\right],
$$

而

$$
\displaystyle
f_Y(y)=
\begin{cases}
\dfrac{1}{2\pi},&-\pi\le y\le\pi,\\
0,&\text{其他}.
\end{cases}
$$

由独立随机变量和的卷积公式，

$$
\displaystyle
f_Z(z)
=\int_{-\infty}^{+\infty}f_X(z-y)f_Y(y)\,dy
=\dfrac{1}{2\pi}\int_{-\pi}^{\pi}f_X(z-y)\,dy.
$$

于是

$$
\displaystyle
f_Z(z)
=\dfrac{1}{2\pi}
\int_{-\pi}^{\pi}
\dfrac{1}{\sigma\sqrt{2\pi}}
\exp\left[
-\dfrac{(z-y-\mu)^2}{2\sigma^2}
\right]dy.
$$

令

$$
\displaystyle u=\dfrac{z-y-\mu}{\sigma},
\qquad dy=-\sigma\,du.
$$

当 $y=-\pi$ 时，

$$
\displaystyle u=\dfrac{z+\pi-\mu}{\sigma};
$$

当 $y=\pi$ 时，

$$
\displaystyle u=\dfrac{z-\pi-\mu}{\sigma}.
$$

因此

$$
\displaystyle
\begin{aligned}
f_Z(z)
&=\dfrac{1}{2\pi}
\int_{(z-\pi-\mu)/\sigma}^{(z+\pi-\mu)/\sigma}
\dfrac{1}{\sqrt{2\pi}}e^{-u^2/2}\,du\\
&=\dfrac{1}{2\pi}
\left[
\Phi\left(\dfrac{z+\pi-\mu}{\sigma}\right)
-\Phi\left(\dfrac{z-\pi-\mu}{\sigma}\right)
\right].
\end{aligned}
$$

</details>

---

13. **【2007-134-11 分】** 设二维随机变量 $(X,Y)$ 的概率密度为

$$
\displaystyle
f(x,y)=
\begin{cases}
2-x-y,&0<x<1,\ 0<y<1,\\
0,&\text{其他}.
\end{cases}
$$

（1）求 $P\{X>2Y\}$；

（2）求 $Z=X+Y$ 的概率密度 $f_Z(z)$。

<details>
<summary>查看答案与解析</summary>

**答案：** （1）

$$
\displaystyle P\{X>2Y\}=\dfrac{7}{24}.
$$

（2）

$$
\displaystyle
f_Z(z)=
\begin{cases}
z(2-z),&0<z<1,\\
(2-z)^2,&1\le z<2,\\
0,&\text{其他}.
\end{cases}
$$

**解析：** （1）事件 $\{X>2Y\}$ 在单位正方形内对应区域

$$
\displaystyle 0<y<\dfrac12,\qquad 2y<x<1.
$$

因此

$$
\displaystyle
\begin{aligned}
P\{X>2Y\}
&=\int_0^{1/2}\int_{2y}^{1}(2-x-y)\,dx\,dy\\
&=\int_0^{1/2}\left(\dfrac32-5y+4y^2\right)dy\\
&=\dfrac34-\dfrac58+\dfrac16
=\dfrac{7}{24}.
\end{aligned}
$$

（2）由 $Z=X+Y$，令 $y=z-x$，则

$$
\displaystyle
f_Z(z)=\int_{-\infty}^{+\infty}f(x,z-x)\,dx.
$$

在联合密度的非零区域内，应满足

$$
\displaystyle 0<x<1,\qquad 0<z-x<1.
$$

当 $0<z<1$ 时，$0<x<z$，故

$$
\displaystyle
f_Z(z)=\int_0^z[2-x-(z-x)]\,dx
=\int_0^z(2-z)\,dx
=z(2-z).
$$

当 $1\le z<2$ 时，$z-1<x<1$，故

$$
\displaystyle
f_Z(z)=\int_{z-1}^{1}(2-z)\,dx
=(2-z)^2.
$$

当 $z\le0$ 或 $z\ge2$ 时，积分区域为空，所以 $f_Z(z)=0$。

</details>

---

14. **【2003-3-13 分】** 设随机变量 $X$ 与 $Y$ 独立，其中 $X$ 的概率分布为

$$
\displaystyle
X\sim
\begin{pmatrix}
1&2\\
0.3&0.7
\end{pmatrix},
$$

而 $Y$ 的概率密度为 $f(y)$，求随机变量 $U=X+Y$ 的概率密度 $g(u)$。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle g(u)=0.3f(u-1)+0.7f(u-2),\qquad -\infty<u<+\infty.
$$

</div>
</div>

**解析：** 按 $X$ 的可能取值进行全概率分解。由于 $X$ 与 $Y$ 独立，条件 $X=1$ 下

$$
\displaystyle U=1+Y,
$$

其条件密度为 $f(u-1)$；条件 $X=2$ 下

$$
\displaystyle U=2+Y,
$$

其条件密度为 $f(u-2)$。

因此

$$
\displaystyle
\begin{aligned}
g(u)
&=P\{X=1\}f_{U\mid X=1}(u)
 +P\{X=2\}f_{U\mid X=2}(u)\\
&=0.3f(u-1)+0.7f(u-2).
\end{aligned}
$$

</details>

---

15. **【2008-134-11 分】** 设随机变量 $X$ 与 $Y$ 相互独立，$X$ 的概率分布为

$$
\displaystyle P\{X=i\}=\dfrac13\qquad(i=-1,0,1),
$$

$Y$ 的概率密度为

$$
\displaystyle
f_Y(y)=
\begin{cases}
1,&0\le y<1,\\
0,&\text{其他}.
\end{cases}
$$

记 $Z=X+Y$。

（1）求 $P\left\{Z\le\dfrac12\mid X=0\right\}$；

（2）求 $Z$ 的概率密度 $f_Z(z)$。

<details>
<summary>查看答案与解析</summary>

**答案：** （1）

$$
\displaystyle P\left\{Z\le\dfrac12\mid X=0\right\}=\dfrac12.
$$

（2）

$$
\displaystyle
f_Z(z)=
\begin{cases}
\dfrac13,&-1<z<2,\\
0,&\text{其他}.
\end{cases}
$$

**解析：** （1）在条件 $X=0$ 下，$Z=Y$，所以

$$
\displaystyle
P\left\{Z\le\dfrac12\mid X=0\right\}
=P\left\{Y\le\dfrac12\right\}
=\int_0^{1/2}1\,dy
=\dfrac12.
$$

（2）按 $X=-1,0,1$ 分解。条件密度分别为

$$
\displaystyle
f_{Z\mid X=-1}(z)=f_Y(z+1),
$$

$$
\displaystyle
f_{Z\mid X=0}(z)=f_Y(z),
$$

$$
\displaystyle
f_{Z\mid X=1}(z)=f_Y(z-1).
$$

故

$$
\displaystyle
f_Z(z)=\dfrac13f_Y(z+1)+\dfrac13f_Y(z)+\dfrac13f_Y(z-1).
$$

三个非零区间依次为 $(-1,0)$、$(0,1)$、$(1,2)$，它们除端点外互不重叠，因此在 $(-1,2)$ 内密度均为 $\dfrac13$。概率密度在有限个端点处的取值不影响分布。

</details>

---

16. **【2016-13-11 分】** 设二维随机变量 $(X,Y)$ 在区域

$$
\displaystyle D=\{(x,y)\mid 0<x<1,\ x^2<y<\sqrt{x}\}
$$

上服从均匀分布，令

$$
\displaystyle
U=
\begin{cases}
1,&X\le Y,\\
0,&X>Y.
\end{cases}
$$

（1）写出 $(X,Y)$ 的概率密度；

（2）问 $U$ 与 $X$ 是否相互独立？并说明理由；

（3）求 $Z=U+X$ 的分布函数 $F_Z(z)$。

<details>
<summary>查看答案与解析</summary>

**答案：** （1）

$$
\displaystyle
f(x,y)=
\begin{cases}
3,&0<x<1,\ x^2<y<\sqrt{x},\\
0,&\text{其他}.
\end{cases}
$$

（2）$U$ 与 $X$ 不相互独立。

（3）

$$
\displaystyle
F_Z(z)=
\begin{cases}
0,&z\le0,\\
\dfrac32z^2-z^3,&0<z<1,\\
\dfrac12+2(z-1)^{3/2}-\dfrac32(z-1)^2,&1\le z<2,\\
1,&z\ge2.
\end{cases}
$$

**解析：** （1）区域 $D$ 的面积为

$$
\displaystyle
S_D=\int_0^1(\sqrt{x}-x^2)\,dx
=\dfrac23-\dfrac13
=\dfrac13.
$$

因为 $(X,Y)$ 在 $D$ 上服从均匀分布，所以区域内的密度常数为

$$
\displaystyle \dfrac{1}{S_D}=3.
$$

（2）先求 $X$ 的边缘密度：

$$
\displaystyle
f_X(x)=\int_{x^2}^{\sqrt{x}}3\,dy
=3(\sqrt{x}-x^2),
\qquad 0<x<1.
$$

固定 $X=x$。由于 $0<x<1$ 时

$$
\displaystyle x^2<x<\sqrt{x},
$$

故

$$
\displaystyle
P\{U=1\mid X=x\}
=P\{Y\ge x\mid X=x\}
=\dfrac{\sqrt{x}-x}{\sqrt{x}-x^2}.
$$

令 $t=\sqrt{x}$，上式可化为

$$
\displaystyle
P\{U=1\mid X=x\}
=\dfrac{1}{1+\sqrt{x}+x},
$$

它随 $x$ 改变，不是常数。因此 $U$ 与 $X$ 不独立。

也可验证

$$
\displaystyle
P\{U=1\}
=3\int_0^1(\sqrt{x}-x)\,dx
=\dfrac12,
$$

而条件概率一般不等于 $\dfrac12$。

（3）当 $U=0$ 时，$Z=X\in(0,1)$；当 $U=1$ 时，$Z=1+X\in(1,2)$。

当 $0<z<1$ 时，只有 $U=0$ 的情形可能满足 $Z\le z$，于是

$$
\displaystyle
\begin{aligned}
F_Z(z)
&=P\{X\le z,\ U=0\}\\
&=3\int_0^z\int_{x^2}^{x}dy\,dx\\
&=3\int_0^z(x-x^2)\,dx\\
&=\dfrac32z^2-z^3.
\end{aligned}
$$

又

$$
\displaystyle
P\{U=0\}=3\int_0^1(x-x^2)\,dx=\dfrac12.
$$

当 $1\le z<2$ 时，所有 $U=0$ 的样本点均满足 $Z\le z$，并且还要加上 $U=1$ 且 $X\le z-1$ 的部分。因此

$$
\displaystyle
\begin{aligned}
F_Z(z)
&=\dfrac12
+3\int_0^{z-1}\int_x^{\sqrt{x}}dy\,dx\\
&=\dfrac12+3\int_0^{z-1}(\sqrt{x}-x)\,dx\\
&=\dfrac12+2(z-1)^{3/2}-\dfrac32(z-1)^2.
\end{aligned}
$$

结合 $Z$ 的取值范围即可得到完整的分布函数。

</details>

---

17. **【2017-13-11 分】** 设随机变量 $X,Y$ 相互独立，且

$$
\displaystyle P\{X=0\}=P\{X=2\}=\dfrac12,
$$

$Y$ 的概率密度为

$$
\displaystyle
f_Y(y)=
\begin{cases}
2y,&0<y<1,\\
0,&\text{其他}.
\end{cases}
$$

（1）求 $P\{Y\le E(Y)\}$；

（2）求 $Z=X+Y$ 的概率密度。

<details>
<summary>查看答案与解析</summary>

**答案：** （1）

$$
\displaystyle P\{Y\le E(Y)\}=\dfrac49.
$$

（2）

$$
\displaystyle
f_Z(z)=
\begin{cases}
z,&0<z<1,\\
z-2,&2<z<3,\\
0,&\text{其他}.
\end{cases}
$$

**解析：** （1）先求 $Y$ 的数学期望：

$$
\displaystyle
E(Y)=\int_0^1y\cdot2y\,dy
=2\int_0^1y^2\,dy
=\dfrac23.
$$

所以

$$
\displaystyle
P\{Y\le E(Y)\}
=P\left\{Y\le\dfrac23\right\}
=\int_0^{2/3}2y\,dy
=\dfrac49.
$$

（2）在 $X=0$ 条件下，$Z=Y$；在 $X=2$ 条件下，$Z=2+Y$。由全概率公式，

$$
\displaystyle
f_Z(z)=\dfrac12f_Y(z)+\dfrac12f_Y(z-2).
$$

当 $0<z<1$ 时，只有第一项非零，

$$
\displaystyle f_Z(z)=\dfrac12\cdot2z=z.
$$

当 $2<z<3$ 时，只有第二项非零，

$$
\displaystyle f_Z(z)=\dfrac12\cdot2(z-2)=z-2.
$$

其余位置密度为零。

</details>

---

18. **【1996-5-9 分】** 假设一电路装有三个同种电气元件，其工作状态相互独立，且无故障工作时间都服从参数 $\lambda>0$ 的指数分布。当三个元件都无故障工作时，电路正常工作，否则整个电路不能正常工作。试求电路正常工作的时间 $T$ 的概率分布。

<details>
<summary>查看答案与解析</summary>

**答案：** $T$ 服从参数为 $3\lambda$ 的指数分布，其分布函数为

$$
\displaystyle
F_T(t)=
\begin{cases}
0,&t<0,\\
1-e^{-3\lambda t},&t\ge0.
\end{cases}
$$

相应的概率密度为

$$
\displaystyle
f_T(t)=
\begin{cases}
3\lambda e^{-3\lambda t},&t>0,\\
0,&\text{其他}.
\end{cases}
$$

**解析：** 设三个元件的无故障工作时间分别为 $X_1,X_2,X_3$。电路要求三个元件同时正常，因此只要任一元件发生故障，电路便停止正常工作，故

$$
\displaystyle T=\min\{X_1,X_2,X_3\}.
$$

对 $t\ge0$，由相互独立性，

$$
\displaystyle
\begin{aligned}
P\{T>t\}
&=P\{X_1>t,X_2>t,X_3>t\}\\
&=\prod_{i=1}^3P\{X_i>t\}\\
&=(e^{-\lambda t})^3
=e^{-3\lambda t}.
\end{aligned}
$$

所以

$$
\displaystyle F_T(t)=1-P\{T>t\}=1-e^{-3\lambda t},
$$

即 $T\sim\operatorname{Exp}(3\lambda)$。

</details>

---

19. **【2002-34-8 分】** 假设一设备开机后无故障工作的时间 $X$ 服从指数分布，平均无故障工作的时间 $E(X)$ 为 $5$ 小时。设备定时开机，出现故障时自动关机，而在无故障的情况下工作 $2$ 小时便关机。试求该设备每次开机无故障工作的时间 $Y$ 的分布函数 $F_Y(y)$。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle
F_Y(y)=
\begin{cases}
0,&y<0,\\
1-e^{-y/5},&0\le y<2,\\
1,&y\ge2.
\end{cases}
$$

</div>
</div>

其中 $Y$ 在 $y=2$ 处有概率质量

$$
\displaystyle P\{Y=2\}=e^{-2/5}.
$$

**解析：** 指数分布的均值为参数的倒数。由

$$
\displaystyle E(X)=5
$$

可知故障率参数为

$$
\displaystyle \lambda=\dfrac15.
$$

设备实际无故障工作的时间为

$$
\displaystyle Y=\min\{X,2\}.
$$

当 $0\le y<2$ 时，

$$
\displaystyle
\{Y\le y\}=\{X\le y\},
$$

故

$$
\displaystyle F_Y(y)=1-e^{-y/5}.
$$

当 $y\ge2$ 时，由 $Y\le2$，必有 $F_Y(y)=1$。在 $y=2$ 处的跳跃大小为

$$
\displaystyle
F_Y(2)-F_Y(2-)
=1-(1-e^{-2/5})
=e^{-2/5},
$$

它表示设备在两小时内未发生故障、因达到规定工作时长而关机的概率。

</details>

---

20. **【2012-3-11 分】** 设随机变量 $X$ 与 $Y$ 相互独立，且都服从参数为 $1$ 的指数分布，

$$
\displaystyle U=\max\{X,Y\},\qquad V=\min\{X,Y\}.
$$

（1）求 $V$ 的概率密度 $f_V(v)$；

（2）求 $E(U+V)$。

<details>
<summary>查看答案与解析</summary>

**答案：** （1）

$$
\displaystyle
f_V(v)=
\begin{cases}
2e^{-2v},&v>0,\\
0,&\text{其他}.
\end{cases}
$$

（2）

$$
\displaystyle E(U+V)=2.
$$

**解析：** （1）对 $v\ge0$，

$$
\displaystyle
\begin{aligned}
P\{V>v\}
&=P\{X>v,Y>v\}\\
&=P\{X>v\}P\{Y>v\}\\
&=e^{-v}e^{-v}
=e^{-2v}.
\end{aligned}
$$

所以

$$
\displaystyle F_V(v)=1-e^{-2v},\qquad v\ge0,
$$

从而

$$
\displaystyle f_V(v)=F_V'(v)=2e^{-2v},\qquad v>0.
$$

（2）对任意两个实数均有

$$
\displaystyle \max\{X,Y\}+\min\{X,Y\}=X+Y.
$$

因此

$$
\displaystyle
E(U+V)=E(X+Y)=E(X)+E(Y)=1+1=2.
$$

</details>

---

21. **【2016-3-11 分】** 设总体 $X$ 的概率密度为

$$
\displaystyle
f(x;\theta)=
\begin{cases}
\dfrac{3x^2}{\theta^3},&0<x<\theta,\\
0,&\text{其他},
\end{cases}
$$

其中 $\theta\in(0,+\infty)$ 为未知参数，$X_1,X_2,X_3$ 为来自总体 $X$ 的简单随机样本，令

$$
\displaystyle T=\max\{X_1,X_2,X_3\}.
$$

（1）求 $T$ 的概率密度；

（2）确定 $a$，使得 $E(aT)=\theta$。

<details>
<summary>查看答案与解析</summary>

**答案：** （1）

$$
\displaystyle
f_T(t)=
\begin{cases}
\dfrac{9t^8}{\theta^9},&0<t<\theta,\\
0,&\text{其他}.
\end{cases}
$$

（2）

$$
\displaystyle a=\dfrac{10}{9}.
$$

**解析：** （1）总体分布函数为

$$
\displaystyle
F_X(x)=
\begin{cases}
0,&x\le0,\\
\displaystyle\int_0^x\dfrac{3u^2}{\theta^3}\,du
=\dfrac{x^3}{\theta^3},&0<x<\theta,\\
1,&x\ge\theta.
\end{cases}
$$

由于样本相互独立，故最大次序统计量 $T$ 的分布函数为

$$
\displaystyle
F_T(t)
=P\{X_1\le t,X_2\le t,X_3\le t\}
=[F_X(t)]^3.
$$

因此在 $0<t<\theta$ 时，

$$
\displaystyle
F_T(t)=\left(\dfrac{t^3}{\theta^3}\right)^3
=\dfrac{t^9}{\theta^9},
$$

从而

$$
\displaystyle
f_T(t)=F_T'(t)=\dfrac{9t^8}{\theta^9}.
$$

（2）

$$
\displaystyle
\begin{aligned}
E(T)
&=\int_0^\theta t\cdot\dfrac{9t^8}{\theta^9}\,dt\\
&=\dfrac{9}{\theta^9}\int_0^\theta t^9\,dt\\
&=\dfrac{9}{\theta^9}\cdot\dfrac{\theta^{10}}{10}
=\dfrac{9\theta}{10}.
\end{aligned}
$$

要求 $E(aT)=\theta$，即

$$
\displaystyle a\cdot\dfrac{9\theta}{10}=\theta.
$$

由于 $\theta>0$，故

$$
\displaystyle a=\dfrac{10}{9}.
$$

</details>

---

22. **【2021-13-12 分】** 在区间 $(0,2)$ 上随机取一点，将该区间分成两段，较短一段的长度记为 $X$，较长的一段长度记为 $Y$，令

$$
\displaystyle Z=\dfrac{Y}{X}.
$$

（1）求 $X$ 的概率密度；

（2）求 $Z$ 的概率密度；

（3）求 $E\left(\dfrac{X}{Y}\right)$。

<details>
<summary>查看答案与解析</summary>

**答案：** （1）

$$
\displaystyle
f_X(x)=
\begin{cases}
1,&0<x<1,\\
0,&\text{其他}.
\end{cases}
$$

（2）

$$
\displaystyle
f_Z(z)=
\begin{cases}
\dfrac{2}{(z+1)^2},&z>1,\\
0,&z\le1.
\end{cases}
$$

（3）

$$
\displaystyle E\left(\dfrac{X}{Y}\right)=2\ln2-1.
$$

**解析：** 设随机点到区间左端点的距离为 $W$，则

$$
\displaystyle W\sim U(0,2),
$$

且

$$
\displaystyle X=\min\{W,2-W\},\qquad Y=\max\{W,2-W\}=2-X.
$$

（1）显然 $0<X<1$。当 $0<x<1$ 时，

$$
\displaystyle
\begin{aligned}
F_X(x)
&=P\{\min(W,2-W)\le x\}\\
&=P\{W\le x\}+P\{W\ge2-x\}\\
&=\dfrac{x}{2}+\dfrac{x}{2}
=x.
\end{aligned}
$$

因此

$$
\displaystyle f_X(x)=F_X'(x)=1,
\qquad 0<x<1.
$$

即 $X\sim U(0,1)$。

（2）由 $Y=2-X$，有

$$
\displaystyle Z=\dfrac{2-X}{X}=\dfrac{2}{X}-1.
$$

当 $0<X<1$ 时，$Z>1$。反解得

$$
\displaystyle X=\dfrac{2}{Z+1}.
$$

由一维随机变量函数的密度变换公式，

$$
\displaystyle
f_Z(z)
=f_X\left(\dfrac{2}{z+1}\right)
\left|\dfrac{d}{dz}\dfrac{2}{z+1}\right|
=1\cdot\dfrac{2}{(z+1)^2},
\qquad z>1.
$$

（3）由于 $Y=2-X$ 且 $X\sim U(0,1)$，

$$
\displaystyle
\begin{aligned}
E\left(\dfrac{X}{Y}\right)
&=\int_0^1\dfrac{x}{2-x}\,dx\\
&=\int_0^1\left(\dfrac{2}{2-x}-1\right)dx\\
&=\left[-2\ln(2-x)-x\right]_0^1\\
&=2\ln2-1.
\end{aligned}
$$

</details>
